# 指纹浏览器 (Fingerprint Browser) — v0.2.0-rc1

基于 **Chromium** 的多账号隔离与管理工具：**指纹身份**、**代理隔离**、**加密凭据/支付保险库**，以及 **AI 自动化执行引擎**（LLM Planner 驱动真实浏览器完成任务：开站、填表、登录、搜索、验证、修复重试、业务结果核验）。

当前版本 `0.2.0-rc1`：v2 任务池（100 真实站点任务、真实 deepseek LLM）**99% SUCCESS + 1% 可信人工升级 = 100% 可接受结局率**（canonical240 基线，run6 2026-09-04）。

> 📖 **使用手册**：每个面板怎么用、常见工作流、FAQ、安全红线 → [docs/USER_GUIDE.md](docs/USER_GUIDE.md)

## 技术栈
- 后端：Node.js + Express + Playwright（控制 Chromium / 自定义 native 构建）
- 前端：React + Vite + TailwindCSS（AI Operator Console 仪表盘）
- LLM：DeepSeek（`deepseek-chat`，Planner / Diagnosis / Repair）
- 存储：本地 JSON（`data/`）+ 可选 SQLite（storeFacade 驱动切换）
- 敏感数据：AES-256-GCM 加密保险库（`server/vault.js`）

## 快速开始
**Windows 一键启动（推荐）**：双击 `start.bat` —— 自动装依赖 → 构建前端 → 生成保险库主密钥（写入 .env）→ 启动服务 → 自动打开控制台 `http://127.0.0.1:8787`。AI 功能在控制台「系统设置」里粘贴 DeepSeek API key 即可（密文落盘、即时生效，无需重启）。

手动方式：
```bash
# 1. 安装依赖（后端 + 前端）
npm run install:all

# 2. 安装 Chromium（仅首次）
npx playwright install chromium

# 3. 配置环境变量：复制 .env.example 为 .env 并填写
#    服务端启动时自动加载 .env（显式环境变量优先，不覆盖）
#    DEEPSEEK_API_KEY=sk-...   ← 也可在 UI「系统设置」配置（推荐）
#    FPB_MASTER_KEY=...        ← 加密保险库主密钥（start.bat 会自动生成）

# 4. 启动
npm run dev     # 开发模式（后端 8787 + 前端 5173 热更新）
npm start       # 仅后端（生产/长跑；自动服务 client/dist 静态前端）
npm run build   # 前端构建（client/dist）
FPB_NO_EMPTY_OUT_DIR=1 npm run build   # 零删除构建（dist 累积后清空动作会被 safe-delete 守卫拦截时用）
npm test        # 全量回归（runRegression.js 271 项 + phase9 246 项双护栏）
```

**一键启动（Windows，推荐）**：双击仓库根 `start.bat` —— 自动完成 Node 检查 → 服务端依赖 → 客户端依赖 → 前端构建 → 生成并写入 `FPB_MASTER_KEY` → 起服务并打开 `http://127.0.0.1:8787`。发布包解压后同样是双击 `start.bat`。

**首次运行就绪度自检**：控制台「就绪检查」页会在进入时自动跑一遍，并在必需项缺失时自动落到引导页（不再让用户猜缺什么）。

| 类别 | 检查项 | 未通过时做什么 |
|------|--------|----------------|
| 必需 | 身份会话 | 本机模式自动引导；多用户模式去「治理中心」建账号与角色 |
| 必需 | LLM 凭据 | 「系统设置 → LLM」填 provider + Key，点「测试连通」（Key 永不明文出站，只回 last4 掩码） |
| 必需 | 浏览器配置 | 「配置管理」新建配置后启动浏览器 |
| 可选 | 指纹模板 / 代理 / 自动化任务 | 缺失不影响启动，只影响对应能力 |

同口径机读端点：`GET /api/settings/readiness` → `stage: READY | SETUP_REQUIRED` + `checks[]`（每项含 `optional`、`hint`、`panel` 用于前端跳转）。

前端控制台：`http://localhost:5173`（开发）/ 后端 API：`http://localhost:8787`。

## 功能总览
| 能力 | 说明 |
|------|------|
| Profile 与指纹身份 | 种子化可复现指纹（UA/UA-CH/screen/时区/语言/字体/WebGL/Canvas/Audio/硬件），headful 真实分辨率回填，headless CDP metrics 对齐；运行态快照（时长/当前页面/页签数/代理，5s 轮询） |
| Native 身份架构（16-B） | **10 个 Chromium native patch 全 ACTIVE**（identity-config-plumbing / navigator.webdriver / navigator.platform / navigator.userAgentData.platformVersion / userAgentMetadata.platform / userAgentMetadata.mobile / cdp-platform-surrender-fallback / navigator.hardwareConcurrency / navigator.deviceMemory / navigator.maxTouchPoints）；`server/fp/nativePatchManifest.js` 为**唯一事实源**（`enabled` 仅在该 patch 走完 source→patch→build→runtime→行为测试→ownership→回归**全链**后才翻真），opt-in 缺省逐字节 stock；languages 走 CONFIG 层 pref 注入三端同源 |
| 代理管理 | HTTP/SOCKS5，出口 IP 预检、代理-指纹一致性（基于 IP 的时区/语言/地理推导） |
| 加密保险库 | 邮箱/密码/卡号/CVV 加密存储；LLM 永不见明文 CVV/卡号（credentialRef + masked） |
| AI 自动化引擎 | Planner→Runtime→Verification→Repair→Escalation 全链：业务状态核验、失败诊断、churn 熔断、replan 契约、凭据启动预检、可信升级（CREDIBLE_BUSINESS） |
| 调度与并发 | 定时触发（固定间隔 + cron 表达式按表不漂移）、批量执行、Worker 池、容量管理、断点续跑、崩溃恢复 |
| 可观测性与控制面 | AI Operator Console：任务时间线、VIL/ESCALATION 节点、指标面板、事件取证；执行引擎面板（调度器控制 / Worker 池 / 队列 / 资源池获取释放 / 提交执行 / 崩溃恢复扫描 / 动作契约与策略只读调试）；数据备份一键导出与全量恢复 |
| 智能记忆 | 站点画像 / 元素记忆 / 流记忆 / 失败知识只读面板 + 经验包导出导入（跨环境迁移）+ Router 决策试算 / 环境推荐 / 经验健康看板（准确率、LLM 节省、Memory ROI、站点×环境矩阵） |
| 治理与合规 | API Keys 自管（明文仅创建时出现一次、只读标记、撤销即失效）、凭据引用注册表（credentialRef 脱敏视图 + 明文就绪状态现算）、安全审计日志（只写不可篡改 + 过滤查询 + JSON 导出）、工作空间与成员 RBAC |
| 任务取证 | 单任务详情：结构化诊断（根因/置信/重试策略/证据/失败快照）、修复尝试与策略成功率、执行记录、动作链重放 |
| 评估基准 | 冻结 v2 任务池（100 任务）+ canonical240 基线 + 双回归护栏（runRegression 272 项 **272/0** 全绿；phase9 **OK=247/BAD=0** 全绿；step22 观测窗（C118）与执行器超时（C119）已按跨层不变量分层重基线；runtime.run() 异常结算作用域修复（C122）+ 文档数值×事实源自动守护（C121）+ 存在性裁决跨层一致性（C124）+ 业务子句覆盖面跨层对账（C125）+ 页面文本证据口径统一（C126）+ 页面文本通道字段名真实性×唯一口径（C127）+ 上下文形状契约真实性（C128）+ 子句跨消费方归一化口径统一（C129）+ `url_contains` 大小写口径跨层统一（C130）+ `url_contains` 的 P2 无效证据守卫统一为唯一实现（C131）+ 4b `targetPresent` 两支口径对账（C132）+ 契约求值收口到唯一实现（C133）+ `stateType` 业务态枚举唯一事实源（C134）+ LLM 调用结果 × 生命周期事件类型解耦（C135）+ 回归命名面唯一事实源与 Worker/Dispatch 契约钉（C136）+ 扫面规则第 3/4 份复刻与终态集合死导出副本收口（C137）+ Flow 重放保真与「不可发生的输入」契约钉（C138）+ F1 零证据拒点双向契约与数据根口径收口（C139）+ repair 策略内部 click 伪验证不可达与 Phase5/Phase23 归因（C140）+ `async` 函数内同步忙等消除（阻塞事件循环 300/600ms → 委托既有非阻塞退避，数值逐值等价）与任务等待原语 4 份复制收口为单一实现（C141）+ 凭据动作判据第二份实现收口（漏判 15 项凭据动作 ⇒ 归零）与同一谓词归一化口径对称化（C142）+ 凭据动作判据族按「内容形状」收口为唯一实现（异名同义第三份漏判 15 项 ⇒ 归零，C143）+ 凭据判据的**本地化写值语义面**（中文凭据字段语义在写值动作上 fail-open —— 真实数据 5 形状 / 128 次出现 ⇒ 归零；按动作类型分层使 click 动作族一格未动，放宽面 0，C144）+ Skill 层 `field_value` 子句的语义接地与可判定性（构建侧「只认 expect + 把 undefined 串进全等比较」两道死条件 ⇒ 176 个真实 Skill 的 `fill = 0`、填表步整段缺失；按 `action.target.field` 接地且不持久化定位符修复 —— 入集面 3983/3983 由全拒变全入集、判定面仅「已填写」FALSE→TRUE，有界放宽，C145）） |

## 发布包（portable）
```bash
node server/scripts/pack_release.js --zip   # 产出 release/identra-vX.Y.Z-win64.zip
```
目标机器只需 Node 18+：解压 zip → 双击 `start.bat`（自动装依赖/构建/生成主密钥）→ 控制台自动打开 → 「系统设置」粘贴 DeepSeek key（即时生效）。打包器含安全红线：`.env` / vault / 运行时设置 / node_modules 一旦检出即 fail-fast，真实凭据绝不随包分发。

## 常用命令
```bash
npm run dev            # 开发：后端 + 前端
npm start              # 仅后端
npm test               # 全量回归（server/scripts/suiteScope.js 唯一事实源自动发现；EXCLUDED_SUITES.json 显式登记排除）
bash server/scripts/run_phase9_regression.sh   # 第二回归护栏（顺序执行）
```

## 架构要点
```
server/
  index.js            # Express API + 静态托管
  browserManager.js   # 浏览器生命周期 / CDP / 指纹接线（C7-CONFIG: languages pref 注入）
  fp/
    generate.js       # 指纹生成（种子可复现）
    inject.js         # JS 注入层（navigator/screen/canvas/WebGL/Audio/WebRTC/UA-CH 回放）
    uaBrands.js       # UA-CH brands 唯一事实源=原生运行时捕获回放（P4.2）
    nativePatchManifest.js  # Native patch 注册表（enabled=已被 POC 证明的数量）
    identity*         # identity schema/factory/store（16-B 身份同源）
  agent/              # AI Operator：planner/runtime/verification/repair/intelligence
  scripts/            # 测试与基准（test_*.js 自动发现；runRegression/phase9/canonical240）
client/               # React 仪表盘
data/                 # 运行时存储（gitignore）
.benchmark/           # 取证与报告（gitignore）
```

## 当前基线（2026-09-16）
- **C140（登记表「待归因（计时 + 语义）」两项 EX-04/EX-05 的归因与修复 —— 挖出 2 处 A 类真缺陷「修复策略核心动作结构性不可达」；生产业务改动仅此 2 处；+ 同源第三例横扫 + C137 观测误归属修正）**：① **计时轴先消掉**：两项的「跑不完」是「未做数据根隔离 + 断言等满观测窗」的**复合效应** —— 补齐隔离行（必须早于**首个 `require`**：`dataRoot()`/`browserManager`/`identityStore` 的数据根在模块加载期解析）后 Phase23 在 305s 内跑完、不再被 SIGTERM。② **★ A 类真缺陷**：`schema/action.js` 的 `MUST_VERIFY` 名单含 `click`，而 `tools.execute` 管线第 1 步（`tools.js:171-172`）会对**每个**动作再跑一次 `validateAction`；`repair/strategies/obstruction.js:25`（7 个 dismiss 语义）与 `sessionExpired.js:22`（自动重登 login 点击）却写 `verification:{type:'none'}` ⇒ **`DISMISS_OVERLAY` 的核心动作与 `REAUTH_OR_PAUSE` 的重登动作恒被判 `ACTION_INVALID`**（探针实测 25 次 attempt 全为该错，与语义层/恢复链**无关**）。改为 `page_change`（该动作的真实效果）——`tools.execute` **不执行** verification（全仓 grep 无 `verification.` 调用点），真实成功门是 `repair/executor.js:64` 的 `step.verification` ⇒ **方向 = 收紧**（动作由恒不可达变可达），断言口径零变化。③ **★ 本批最「哑」的一处：终态词表把非终态当终态**。`taskStateManager.TASK_TERMINAL` **不含** `PAUSED_FOR_HUMAN`（源码注释：「pauseForHuman 是等待人工，**可 resume**」），但旧 `waitStatus` 词表把它写了进去 ⇒ **在中间态提前返回**，随后读到的是 **TOCTOU 竞态读**。探针时间线实测：`RUNNING → PAUSED_FOR_HUMAN(69.2s) → RUNNING(70.5s) → SUCCESS(73.8s)` —— 中间态窗口 **1.27s ≫ 600ms 轮询**，故**读到哪个值取决于时序**；这也是「两次运行给出相反读数」的根因（修复前一次断言读到 `HUMAN_ESCALATION`，extra 里是 **`escalate` 才写**的根因文案而 `pauseForHuman` 写的是 `error:null`；修复后一次读到 `PAUSED_FOR_HUMAN`）⇒ **同一缺陷的两个读数，任何单点修复都会在下一次翻面**。机制：`repairManager` 修复耗尽 → `pauseForHuman`（中间态）→ `runtime.js:1171` 判定 replan 候选并**基于实况重规划续跑** → 耗尽/熔断才在 `runtime.js:1205-1210` `escalate` 写 `HUMAN_ESCALATION`。④ **横扫同族调用点**：`waitStatus` 是**共享原语**（5 个套件各自复制），族级收口为**派生唯一事实源** `TASK_TERMINAL`（Phase5/22/23/31），结构性消除「非终态被当作等待目标」+「漏新增终态导致空转满观测窗」；**唯一合法例外**是 `testAgentPhase4.js` 等 `PAUSED_FOR_HUMAN` 的**审批流**（HIGH 动作默认暂停 → approve → 续跑，是**真实停驻态**），且属登记排除项 ⇒ 显式登记不改（防后续「一致性横扫」误改）。⑤ **同源第三例**：`testAgentPhase22.js` 的 click 夹具同样写 `verification:{type:'none'}`，长期隐身是因为断言**不依赖点击成功**（只判终态与诊断四层）⇒ 用例自称的「点击不存在元素 → `ELEMENT_NOT_FOUND` → 诊断」**从未真正执行**（实际失败是校验层 `ACTION_INVALID`）；修复后实测 **13/0**（100s —— 由 8.2s 变长**正说明点击真的发生了**）。⑥ **`/flaky4` 的慢请求数是旧恢复预算的快照**：真实恢复预算 = `1 + stepMax(默认 3)` 次导航 + `RELOAD_CAP_PER_STEP=1` ⇒ 请求上界 **5**，旧参数 `n<4` 让确定性恢复在**第 4 次**导航就成功 ⇒ Repair 层**恒不被触达**（实测 `repairs2=[]`）⇒ 夹具参数改 6，并由**跨层不变量**（三个数各从源码解析）守护，禁止再手写魔数。⑦ **`/cookie` 的 `DISMISS_OVERLAY` 期望原理上不可能成立**：`repairManager.js:99-101` 对 `VERIFICATION_FAILED` **强制**改写诊断类别（Phase 7 Step 5 的**有意**规则：禁止被误分类后误用 `SEMANTIC_RELOCATE`）⇒ 只可能 `VERIFY_RETRY`；断言改锚「强制路由规则仍在场」这条**真契约**（非放宽）。⑧ **`/renamed` 夹具无法诚实验证**（按钮**无 `onclick`**，点击无任何页面变化）⇒ 新增可验证夹具 `/renamed-nav`（同文案 `Continue`、点击**真实跳转**）+ `/flaky4-reset`（夹具跨次运行计数器复位 —— 否则第 2 次运行恒快、断言**静默变红**）。⑨ **★ C137 观测误归属已修正**：旧 EX-04 条目引用的 `产生 DISMISS_OVERLAY / WAIT_RETRY_RELOAD RepairAttempt` **实属 Phase23**；旧 EX-05 引用的 `Phase 3.4 结果: 24 通过 / 3 失败` 与 region=US/HK 选环境 **实属 Phase34**（C139 已归因晋升）—— 两处均可被 grep **唯一命中**反驳；教训（观测必须可溯源到具体套件，不能凭记忆归类）已写入登记表 `_说明`。⑩ **工程缺陷**：`.gitignore:_*.js` 的「一次性脚本」通配**误伤常驻共享依赖** `server/scripts/_testSite.js`（`testAgentPhase5/22/23/31.js` 四个**已入库**套件都 `require('./_testSite')`）⇒ 新 clone 跑任何浏览器套件直接 `MODULE_NOT_FOUND`，且 test-site `VERSION` 是**两处必须成对**演进的常量（其中一处不入库则配对无保证）⇒ 加例外行 `!server/scripts/_testSite.js`，**必须位于最后一条 `_*.js` 之后**（gitignore 语义是「最后一条匹配者胜」，候选守护 E1d 专钉顺序）。⑪ **实测**：Phase5 **20/0**（72s；原 180s+ 被 SIGTERM）、Phase23 **22/0**（**184s**；原 18/4）、Phase22 **13/0**（100s）、Phase31 28/0（27s）；新守护 `test_c140_repair_action_contract` **51/0**（A 真实调用策略自造动作过生产 `validateAction` / B 源码形状双向咬 / C 族级等待口径 + 三处 click 夹具 + revert 与 missing 对照 / D 跨层预算不变量 / E `.gitignore` 顺序与仓库自洽）；`test_c119_suite_timeout_consistency` **28/0**（补 `waitStatus` 族窗提取 —— 旧提取器只认 `waitTaskTerminal`，这 4 个套件的窗对本守护**结构性不可见** —— 加 A6 提取自洽）；`test_c135_regression_scope` **24/0**（晋升 **18** 项）；**no-op 双向 11/11 全部咬住**（每条「注入即红 + 还原 sha256 逐字节一致」，含**跨守护** c119 A6）。⑫ **执行器保险丝**：Phase23 修复后 **184s > 全局 180s** ⇒ 不加 override **必然**被 SIGTERM（而该假红极易被误读成代码缺陷）；按 C119 先例登记 `300000`（1.63× 实测、2.5× 单窗；**仅放宽保险丝**，观测窗与断言一字未动、超时不参与业务判定）；同批按同一判据核过 Phase5 72s（2.5×）/Phase22 100s（1.8×）/Phase31 27s（6.7×）⇒ 均不登记。⑬ **划分**：候选 **269** = 入集 **266** + 登记排除 **3**（Phase2/Phase3 账号 402、Phase4 需自起后端 7788），`phase9Scope=241`；登记表 **5 → 3**（删除条目后 `JSON.parse` 实读核验：`roundTrip=true`、`U+FFFD=0`、无残留/无缺字段）。**回归**：runRegression **266/0**（1455.1s）+ phase9 **OK=241/BAD=0**，零污染（精确等于计划改动集 + 还原 `phase12_pool_v2.json`）。**诚实声明**：本批**不承诺成功率提升**（生产改动仅两处**死路径**的能力恢复）；`maxReplans:0` 是「让用例只覆盖它自称的分支」而非放宽（断言由「接受两态之一（含非终态）」**收紧**为「必须停在指定真终态且带根因」）；`testAgentPhase2.js` 的**第三个**等待原语 `waitForStatus` 词表同样含非终态，但该套件是登记排除项（LLM 402 不可用）⇒ 无法验证修改后果，**只登记不改**。

- **C139（`testAgentPhase31` / `testAgentPhase34` 双红项归因 —— 结论：**均为测试自身缺陷，生产业务零改动**；+ F1 零证据拒点双向契约 + 数据根口径收口 + 2 项晋升）**：① **Phase31 属测试过时**：旧 fixture 用 `{semantic:'submit'}` + 标签 `Continue`/`Proceed`，建于 **C105 F1 之前**的可行性假设（当时「纯图标/无文本按钮兜底」给所有 button 一律 0.4 同分、DOM 顺序决胜）；F1 已按真实站点实证（法语站误点 Plateforme 菜单，真实 CTA 一直在 index 40）改为「兜底候选必须与元素**自身身份信号**有词法关联」⇒ 该 fixture 恒零候选（探针实测 `semanticResolver.resolve → n=0`、`resolveSelector → null`；标签换成 `Submit` 即 `n=1`）。另修两个**测试自身**缺陷：**缺 null 守卫**（`r1.pattern` 抛 `TypeError` 把 Case2/3 与整段集成静默吃掉 ⇒ 长期「只报 1 项红 + 系统性瘫痪」）、**异常路径自赋值 no-op**（`resolve = resolve`，spy 不还原）。② **集成段的真实链路（实测取证）**：F1 拒点 → 恢复链 `elementMissing` 用 `SYNONYMS['submit']` 展开变体、挑出**在新鲜观察中可解析**的那个 → 业务 SUCCESS，且记忆按**恢复链生效语义**落库（`localhost|continue|CTX:…`，**不是**计划语义 `submit`）⇒ 记忆断言改为 **key 无关**形态（按观察标签统计 pattern 积累与演化），并把「零证据语义不复用记忆」钉成**显式契约**（F1 若被无意识回退即变红）。③ **Phase34 属数据根口径不唯一**：`realProfileIds()` 硬编码 `__dirname/../../data/profiles.json`，是**第二份数据根口径**（不随 `FPB_DATA_DIR` 走）⇒ 隔离只挪走 store 集合，白名单仍指向真实目录 ⇒ 遗留真实 profile 进入 region 候选组。收口为 `dataRoot()`（**无隔离时与旧路径逐字相同 ⇒ 行为中性**）＋ 症状直钉（隔离下白名单空集 / clean() 后无遗留非 fixture 记录）。④ **实测与证据**：Phase31 **28/0**（26s，含三次真实浏览器任务）、Phase34 **31/0**；no-op 双向 **4/4 ALL OK**（移除隔离行 / 回退硬编码路径 / 回退零证据 fixture / 移除 null 守卫退回崩溃，均「注入即红 + 还原 sha256 逐字节一致」）；新守护 `test_c139_fixture_contract_and_isolation` **36/0**；登记表 **7 → 5**。
- **C138（`testAgentPhase32` 的 Flow 复用红项归因 —— 结论：**测试构造了生产不可发生的输入，生产零改动**；+ 三层纵深契约守护 + 1 项晋升）**：① **红项形态**：`B2 Case2 二次同目标跳过 LLM` 实测 `fromFlow=false calls=2`（而前序 `B1 落库 flow conf≥0.85` **通过**）⇒ 表观矛盾「flow 已落库却不被复用」，C137 曾登记为「可能为真缺陷（高优先级）」。② **决定性探针（逐段取证）**：`lookup(site,goal)` **命中**（`reused=true conf=0.925`）、`getByKey` 亦命中 ⇒ **不是键构造/新鲜度问题**；真正断点在 `flowPlanner.tryFlowPlan` 的 `validatePlan(flowMemory.toPlan(...))` **判否**：`steps[1](fill_email): action 非法: fill 必须提供 value 或 credentialRef`（规则源 `schema/action.js:174`）⇒ 静默降级 LLM（CAP-K1 设计即「绝不带病重放」）。③ **归属证明（反证法）**：把同一用例的 `LLM_PLAN` 的 fill 补成**合法契约**（身份类字段按 P1 用 `credentialRef`）后**整条链转绿**（`fromFlow=true / calls=1`），`credentialRef` 往返保真、无 selector/坐标 ⇒ **复用链路健康**。而该非法形态**在生产不可能出现**：`planner.planObjective` 出口强制 `validatePlan(canonical)`（`planner.js:465`）⇒ 这种 plan 进不了系统、也永远落不成 flow；旧用例把**整个** `planObjective` 换成了 stub，恰好绕过了那道校验 ⇒ **测试缺陷，非真回归**（L16「为不存在的形状写断言」在测试侧的实例，与 C136 同类）。④ **修复（只改测试）**：Ⅰ `LLM_PLAN` 的 fill 合法化（补 `credentialRef`，符合「LLM 不见明文凭据」红线）；Ⅱ 补两条**正向**断言（比原断言更强，非放宽）—— **写读保真**（往返后 `action.type` 仍是 `fill` 而非退化为 click + `credentialRef` 原样带出）与 **契约守卫**（去掉取值字段后，**置信度 0.925 达标也必须被 `tryFlowPlan` 拒绝**，把安全属性从「偶然红」变「显式守护」）；Ⅲ 补数据根隔离（本套件 `clearAll()` 会清空三个 AI 记忆集合 ⇒ 此前直接写真实 `server/data`）⇒ 晋升入集。实测 **25/0**（原 22/1）。⑤ **新守护 `test_c138_flow_replay_fidelity`（23/0，A–E 组）**：**A「不可发生的输入」**（planner 出口终态门静态定位 + 契约层拒绝无取值 fill + **拒绝敏感字段 value 字面量** + 反向「合法 fill 必须过」防假绿 + 回归用例 fixture 自身合法性）；**B 写读保真（CAP-K1 核心）**（动作类型不退化 / `credentialRef` 往返 / 重建后仍过校验 / 无 selector·坐标 / 非敏感 value 往返 + **敏感明文绝不落库** + **读侧纵深**：敏感 flow 即便落库，重建后无取值 ⇒ 仍被 `validatePlan` 拒绝）；**C 复用入口守卫**（合法高置信 ⇒ 命中且 **flowId 回指落库记录**〔防真空绿〕/ **高置信度但非法 ⇒ 必须拒绝** / 低置信 ⇒ 不命中〔阈值未下调〕/ 阈值锚唯一事实源 0.85）；**D 单一事实源**（`toPlan` 全仓仅 1 实现 / 读侧「重建后校验」仅 `flowPlanner` 1 处）；**E 隔离自证**。⑥ **§D no-op 双向 7/7 全咬**：短路读侧 `validatePlan`（咬 C2+D2）/ 阈值 0.85→0.5（咬 C4）/ `toPlan` 丢 `credentialRef`（咬 B1b/B1c/C1）/ 动作类型退回一律 click（咬 B1/B2/B3b）/ 用例 fill 退回无取值（咬 A4）/ 写侧敏感剥离失效（咬 B3/B3b）/ 植入第二份 `toPlan`（咬 D1）⇒ 注入即红、还原后 **sha256 逐字节一致**且回绿，`ALL OK`。⑦ **双回归**：`runRegression` **260/0**、`phase9` **OK=239/BAD=0**；划分 **候选 267 = 入集 260 + 登记排除 7**。⑧ **诚实声明**：**生产代码零改动**（只改 1 个测试 + 新增 1 个守护 + 登记表/清单维护），**不承诺任何成功率提升**（未跑 100×3）；方向 = **修断言使其对齐生产契约**，而非放宽门槛。
- **C137（两处「同域副本」收口：扫面规则的第 3/4 份复刻 + 终态集合死导出副本；生产零改动）**：① **扫面规则副本面**（B 类·一致性）：`run_phase9_regression.sh:46` 一直内联 `for f in $(ls server/scripts/test_*.js | sort)` —— 与 C135 建立的唯一事实源 `scripts/suiteScope.js`（runRegression 入集）**并列的第三份扫描规则副本**；`test_c94_safe_port.js` 的 P3「整类守卫」另有内联 `/^test_.*\.js$/` = **第四份**，其后果是「整类杀手」这个自称与实现**不等价**：`testAgent*.js` / `testMemoryIsolation.js` / `testWorkerIsolation.js` 等 21 个新式命名**从未被这组安全检查过**（L14 覆盖面静默漂移）。② **收口（行为中性，非扩张）**：`suiteScope.js` 新增 `PHASE9_RE` + `phase9Scope()`（= 候选 ∩ `server/scripts` ∩ 历史命名 − 登记排除）与 **CLI `--phase9`**；shell 改为 `FILES="$(node server/scripts/suiteScope.js --phase9)"`，取不到或返回空清单 ⇒ **fail closed**（`exit 1` + 致命提示；**绝不回落内联 glob**，也拒绝「正常退出但一个套件都没跑」的静默假绿）—— 与既有 `tmpEnvGuard.js` 同一模式（判定权交给 Node 侧唯一事实源）。**等价性已实测**：`phase9Scope()` = `ls server/scripts/test_*.js` = **238**，逐项一致（守护 C4 **真执行** shell 的获取块取清单再比对，非复刻）。③ **c94 P3 方向 = 严格收紧**：改用 `suiteScope.collectCandidates()`（候选全集 ⊇ 历史命名集，实测 **266 ⊇ 245**）⇒ 覆盖面**扩张**而非缩窄；实测全量复跑 **43/0**（证明其余 21 个套件本就无裸 `listen(0)`，扩张是安全的正向）。④ **终态集合副本**（B 类）：`server/agent/successMetrics.js:11` 内联 `TERMINAL` 与 `taskStateManager.js:14 TASK_TERMINAL` **同域同集合、仅顺序不同**，且全仓扫描**零消费者（死导出）**⇒ 当下影响为零但属典型漂移点 ⇒ 改为**静态委托**（`const { TASK_TERMINAL: TERMINAL } = require('./taskStateManager')`，**同一数组引用**；`taskStateManager` 零 `require` ⇒ 无环）。**仍并存的 3 份副本**（`phase9Benchmark.js:81` / `phase10Benchmark.js:105` / `phase9_gate_replay.js:171`）受 **Phase 6 冻结**（改动静默失效 100×3 基座的风险 > 收益）⇒ 按 L15「并存须可区分＋登记」**显式登记 + 集合等价断言**：成员漂移即红（实测 3 份均集合等价）。⑤ **新守护 `test_c137_scan_rule_and_terminal_singleton`（33/0）**：A 事实源 CLI 可执行/非空/路径形态/有序（+ 防空断言）；B **规格双录入**（独立扫描器算出的集合必须 = 实现，并与入集构成**无黑洞划分** 238 + 20 = 258，且「差集每项的理由必须成立」）；C shell **静态无 glob + 真执行其获取块**（哨兵提取**真实代码**而非复刻）+ **fail closed 行为**（伪装 `NODE` ⇒ 非零退出）；D 静态委托 + 登记副本集合等价；E c94 委托与**覆盖面严格扩张**（非 no-op 美化）；F **反向咬 4 条**（合成内联 glob / 合成内联 `^test_` / 漂移字面量 / 历史集与 phase9 面等价性）；G 隔离自证。⑥ **§D no-op 双向 5/5 全咬**：J1 shell 退回内联 glob（咬 C1）/ J2 CLI 静默返回空清单（咬 C4）/ J3 `successMetrics` 退回字面量（咬 D1）/ J4 c94 退回内联 `^test_`（咬 E2）/ J5 登记副本去掉成员（咬 D6）⇒ 注入即红、还原后 **sha256 逐字节一致**且回绿，`ALL OK`。⑦ **本批另实测归因 4 项登记排除**（补足登记表证据）：`Phase5` / `Phase23` 隔离后 **240s 未跑完**（浏览器集成，EXIT=124）；`Phase31` **测试自身 TypeError**（`:104` 读 `null.pattern`，崩溃前已 1 项红）；`Phase32` 隔离后 **22/1**（唯一红 = `B2 Case2 二次同目标跳过 LLM`，实测 `fromFlow=false calls=2` ⇒ **命中 Flow 复用路径，待定级（可能为真缺陷）** ⇒ 新 C138 候选）。⑧ **双回归**：`runRegression` **258/0**、`phase9` **OK=238/BAD=0**；划分 **候选 266 = 入集 258 + 登记排除 8**。⑨ **诚实声明**：**生产代码零改动**（只改 1 个 agent 常量的**来源** + 执行器/守护脚本），**不承诺任何成功率提升**（未跑 100×3）；本批唯一的覆盖面变化是**扩张**（c94 P3 由历史命名扩到候选全集），扩张后实测全绿。
- **C136（C135 遗留「可能为真回归」两项的最终归因 —— 结论：**测试自身缺陷，生产零改动**；+ 契约守护 + 2 项晋升）**：① **EX-09（`testAgentPhase42` Case3）**：`workerHeartbeat.scan` 自 **Phase 5.8 Finding #1** 起**只对 ASSIGNED/RUNNING 判定死亡**（源码注释原文：空闲 READY/STARTING 本就没有持续心跳，误判 DEAD 会清空 capacity 造成饿死），而旧 Case3 把 worker 留在 **READY** 就拨 40s 前心跳 ⇒ **不在候选集内** ⇒ 永不产出 DEAD（断言恒红）。修法 = **跟随该生产收紧调整测试**（推进到 RUNNING 后再拨心跳），**非修生产**。② **EX-10（`testAgentPhase43`）**：`SchedulerLoop.start()` 第一步即 `_reapZombieDispatches()`（把所有已有 dispatch `cancel`、非终态 task `fail`，`schedulerLoop.js:71/87`），而旧套件**先 `submit()` 后 `start()`** ⇒ 刚入队的 dispatch 立刻被收割成 `CANCELLED`（实测 `workerId:null`、`created→finished` 仅 ~200ms，逐字段吻合）⇒ Case1/3/5/6 恒红。**归属证据（决定性）**：`_reapZombieDispatches` 在 `2c27e9e`（C134）**已存在**且 C135 对本文件的 diff **零命中**该函数 ⇒ **非 C135 引入、非真回归**。③ **同时修掉三处「不可观测形状」断言（比原断言更强，非放宽）**：**Ⅰ** Case1 原断言 `status === 'STARTED'` —— 无浏览器环境下 dispatch 全链仅 ~20ms（实测 `startedAt→finishedAt`=21ms），50ms 轮询**原理上抓不到**瞬时状态 ⇒ 改用**时间戳链**（`assignedAt`+`startedAt` 有值 + 绑定到真实存在的 worker）判定「确实被派发并开始执行」；**Ⅱ** Case3 原断言「A 在跑 ∧ B 仍 QUEUED」**原理上不可成立**（A 一结束 capacity 释放，B 立刻被派发），且旧 `running<=1` 是单点快照（读到全 FAILED 时 `running=0` ⇒ **真空绿**）⇒ 改为**从落库时间戳重建时间线**，以每个 dispatch 的 `[assignedAt, finishedAt]` 为占用区间求**最大重叠数**并断言 `≥1`（防真空绿）且 `≤1`（capacity=1）—— 完全确定性、无采样竞争（外部 10ms 采样实测仍整段漏掉，`peak=0` 假红）；**Ⅲ** Case6「崩溃前残留 STARTED」在当前环境不可构造 ⇒ 改锚**等价且可观测**的不变量「A 已进入执行（`startedAt>0`）∧ 崩溃后未被重置为 QUEUED」。另 Case5 删除「pause 前先提交 b」使「暂停后 b 保持 QUEUED」**可被证明**。④ **结果**：`testAgentPhase42` **18/0**、`testAgentPhase43` **19/0**（连跑 4 次稳定）⇒ 两项由「待归因（可能为真回归，高优先级）」**晋升入集**（均补数据根隔离）。⑤ **新守护 `test_c136_worker_dispatch_contract`（14/0）**：把 Phase42/43 现在**直接依赖的两条生产契约**钉住 —— 契约①`scan` 候选集恰为 `{ASSIGNED, RUNNING}`（静态 + 行为双向：READY 不判死 / RUNNING 判死 / 心跳新鲜不判死）；契约②`start()` 先收割已有 dispatch（行为双向：**先 submit 后 start ⇒ CANCELLED** / **先 start 后 submit ⇒ 不被收割且真实派发**）。若未来有人把①放宽或把②的收割挪出 `start()`，**契约漂移本身先红**，避免被误诊成「测试过时」。⑥ **§D no-op 双向 5/5 全咬**（放宽候选集 / `scan` 恒不判死降级 / 破环 `start` 收割 / 回退 Phase42 修复 / 回退 Phase43 顺序）⇒ 注入即红、还原后 **sha256 逐字节一致**且回绿。⑦ **双回归**：`runRegression` **257/0**（770.8s）、`phase9` **OK=237/BAD=0**；划分 **候选 265 = 入集 257 + 登记排除 8**（`Phase42/43` 出表）。⑧ **诚实声明**：本批**生产代码零改动**，只修测试与登记；**不承诺任何成功率提升**（未跑 100×3）。**方向 = 修断言（对齐可观测性）而非放宽门槛**。
- **C135（LLM 调用结果被编码进生命周期事件类型 ⇒ Worker 提前释放；扫描规则三处副本收敛）**：① **A 类铁证**：`agent/llm/provider.js` 用 `type: ok ? 'agent.tool_result' : 'task.failed'` 上报**LLM 调用结果** ⇒ 一次 LLM 失败（如 `HTTP 402`）即广播 `task.failed`；而 `schedulerLoop._ensureListening()` 四分支**无守卫** ⇒ `_onTaskDone` 把仍在运行的 dispatch 标终态并 `pool.onTaskFinished` **提前释放 Worker**（aiEvents 实证 2 例 `task_mu3ajlrr36uw9` / `task_mu3apcnzcml59`）。② **修复（两层 + 白名单）**：收口为**单一遥测类型 `agent.llm.call`**（`ok` 降为 payload **属性** —— 编码进事件类型正是缺陷成因）+ `events.js` 白名单登记 + `schedulerLoop` **结构守卫**（事实源 = `aiTasks` 持久化状态 ∧ `taskManager.isTaskTerminal`）；依据 = taskManager 全部 **5 个**终态发出点均「先 `_setTaskState` + `store.upsert`、后 `emit`」⇒ 守卫对真实终态**恒真**（生产行为零变化，仅丢弃伪终态）。产出侧 `events.on(` 全仓**仅 1 处**（`schedulerLoop.js:179`）⇒ 本缺陷类无第二处。③ **B 类铁证**：旧扫描规则 `^test_.*\.js$` 漏 21 套件，且实测 **21/21 未做数据根隔离**、**19/21 直接写真实 `server/data`** ⇒ 入集前提是**隔离**而非命名，**盲目放宽规则 = 数据破坏**。④ **修复**：唯一事实源 `scripts/suiteScope.js` + 登记表 `scripts/EXCLUDED_SUITES.json`（10 项显式排除，各带 原因/归类/晋升条件/追踪）+ `runRegression --list` 只读划分 + 表头「差异不隐身」清单；**第三份规则副本已消除**（`test_c119` 内联 `listSuites()` 改为委托事实源）。⑤ **实测划分**：候选 **262** = 入集 **252** + 排除 **10**；**11 项晋升**（`testAgentInfra` / `Phase22` / `Phase33` / `Phase35` / `Phase36` / `Phase41` / `Phase44` / `Phase45` / `Phase46` / `MemoryIsolation` / `WorkerIsolation`，全部补数据根隔离行且插在**首个 `require` 之前**）+ **2 项新守护**。⑥ **双回归**：`runRegression` **254/0**（1083.9s）、`phase9` **OK=236/BAD=0**（234 基线 + 2 新守护）。⑦ 新守护 `test_c135_llm_event_semantics` **23/0**、`test_c135_regression_scope` **24/0**；**5 条 no-op 注入全部咬住**（伪终态丢弃 / 守卫中和 / 规则退回窄口径 / 登记表幽灵条目）。⑧ **诚实声明**：本批另修两处**过时断言**（`testAgentInfra` 的 `autoPayment` —— 它**并未移除**，是支付类唯一放行开关且受 `policy.js:18-24` **环境护栏**约束，旧断言在非测试环境**必然红**；`testAgentPhase22` 的等待集合 + 90s 观测窗 —— 修的是断言而非门槛）。**方向 = 收紧（伪终态不再驱动终态迁移）+ 覆盖面入集前提显式化**。
- **C134（`stateType` 业务态枚举的**唯一事实源**收口 —— 四处字面清单漂移 + 1 项生产越界；1×B 自相矛盾 + 1×L14 多清单漂移 + 1×L15 多数派裁决 + 1×L17 双向咬 + 2×C 类登记）**：① **结构事实**：`stateType` 的裁决权威是 `contract.js:STATE_TYPES`（12 项），`schema/action.js:162` **require 同一常量**（非副本）⇒ 白名单唯一；但库内另有**四处字面副本**：`contract.js:15`（头部注释）、`planner.js:102`（`ACTION_CONSTRAINTS`，structured fallback 路径）、`plan.js:180`（`INSTRUCTIONS`）、`plan.js:250`（`PLAN_STRICT_INSTRUCTIONS`）—— 即 **三处 prompt 同步点**；真实 LLM 经 `deepseek.js:106` 拼接 `PLAN_STRICT_INSTRUCTIONS` **继承**该清单（`deepseek.js` 全文零次 `stateType`，**不构成第五处**）。② **实测铁证 1（F1，B 类，生产可达）**：`ACTION_TO_STATE.uncheck.stateType = 'UNCHECKED'` 而白名单**无此项** ⇒ `deriveContract` 的产物被**自身校验器** `validateContract` 拒绝（`stateType 非法: UNCHECKED`），且 `normalizeContract:215` **静默降级为 `GENERIC_STATE`**；探针 B 组**逐一列示** 10 个 `DERIVABLE` 动作 ⇒ **非法 1/10，唯一异类**。可达性：`verification.js:254 buildEffectiveVerification()` 的 `deriveContract(action)` 是**生产主链路**。③ **实测铁证 2（F2，B 类）**：四处字面清单**均 11 项、均缺 `LOGOUT_SUCCESS`**，而白名单 12 项 ⇒ prompt **明示禁止** `LOGOUT_SUCCESS`、校验器**却允许**：自相矛盾（同模式事故**第四次**，前三次见 `plannerContractText.js` 头注）。④ **修法裁决（L15 多数派 = 事实源）**：`CHECKED`/`SELECTED`/`FIELD_FILLED` 均为元素级结果态且全在白名单内，`uncheck` 与 `check` **结构对称**；且白名单**本就含 3 项无人生产的成员**（`CONFIRMATION`/`DOWNLOAD`/`CUSTOM`，探针 A4）⇒ 其性质是**闭集词表（⊇ 可生产集）**，故**补登 `UNCHECKED`**（12→13）而非把生产者降级（语义撒谎）。⑤ **修复（4 处、3 文件 `+27/−6`）**：`STATE_TYPES` 补 `UNCHECKED`；`planner.js:102` / `plan.js:180` / `plan.js:250` 全部改为**从 `STATE_TYPES` 派生**（`join(' / ')` / `join('|')`），两文件新增 `require` 同一常量；`contract.js:15` 注释改为**指针**（结构性消除第四份同义清单）。**方向 = 词表放宽 1 项 + 清单一源化**。⑥ **门槛未降证明（行为探针，注入桩 `clauseVerify`）**：`evaluateContract` 只消费 `requiredEvidence`/`forbiddenEvidence`/`evidenceLogic`/`confidence`/`timeout`/`allowedAlternatives`，**从不读 `stateType`**；仅改 `stateType` 的三个契约（含**白名单外**值）判定结果**逐字段完全相同** ⇒ 词表扩员**不可能**改变任何判定门槛。唯一可观测差异 = `contract.js:287` 命中替代态时的**证据文案**由 `... matched: GENERIC_STATE` 变为 `... matched: UNCHECKED`（全库无测试断言该文本）。⑦ **2×C 类登记（不可达；诚实声明零当前产品收益）**：`STATE_BY_OBJECTIVE` 的 `DOWNLOAD` 行**无对应 base** ⇒ `:186 find()` 落空 ⇒ 恒等价于不存在（**保留为待接线占位**；不新增 `download` 生产者——那会改变主链路派生面，属行为变更）；同一表**顺序反向** ⇒ 「退出登录」实得 `LOGIN_SUCCESS`（与意图相反）⇒ 已把 `LOGOUT` 行调至 `LOGIN` 行**之前**。**不可达依据**：`contractFromObjective` 在 `server/agent` 下**仅 `:189` 自调用、生产零外部调用**（探针 D 组）。⑧ **★no-op 双向**：回退三文件（`git show HEAD:` + `cp`；**禁用 `git stash`**）⇒ 守护 **30/46（16 红）**，红项**逐条命中预期**（B1/B2/B3b = F1、C1–C5 = F2、D1/D3 = F4、F3/F4/F4b/F5/F5b/F6 = 残留扫描）；探针回 **1/10 非法**；恢复后 sha256 **逐字一致**（`contract 2c761662…`、`planner 96b68ebe…`、`plan 0fb99063…`）＋ **46/46**。⑨ **干系套件扫面（改前必做）**：**25 个老守护零假红**（`test_a1_navigate_fill_guard`、`test_c106_f22_contract_sync`、`test_planner_contract_sync|p4|p6`、`test_credential_*`、`test_evidence_*`、`test_c108`、`test_phase11_business_contract`、`test_c123`–`test_c134`）；★新增 `require` 的位置经约束确认 —— `test_c106_f22_contract_sync.js:62` 按 `indexOf('const ACTION_CONSTRAINTS')` **切源码片区**，故 `require` 必须置于该行**之上**。⑩ **自捕获探针缺陷（如实登记）**：守护首版 F4/F4b 断言写了**臆造形状**（`STATE_TYPES\s*=\s*require`）而**误红**（真实形状是 `const { STATE_TYPES } = require`）⇒ 修正后补 F4c**分辨力自证**（正则对臆造形状必须失配）—— L16「为不存在的形状写代码」在**守护自身**上的实例。守护 `test_c134_state_type_enum_parity` **46/0**（A 唯一事实源+反真空 / B 生产⊆白名单+revert 分辨力自证 / C 三处 prompt 求值后字符串+反向探针 / D 顺序不变量+登记型断言 / E 门槛未降行为等价 / F 残留扫描+正则分辨力自证）。回归 **241/0（793.7s）** ＋ phase9 **OK=234/BAD=0**（+1 = 本批新守护）。
- **C133（契约求值在 VIL 的**第二份实现**：`businessStatePresent` 完全缺 `forbiddenEvidence` 通道；1×A 跨层相反答案 + 1×L6 同语义双实现 + 1×L15 + 1×L16 等价性登记）**：① **结构事实**：契约求值有**两份实现** —— A 裁决面 `verification.js:verify` → `contract.evaluateContract`（**唯一实现**），C 诊断面 `verificationIntelligence.businessStatePresent` 是**手写第二份**（逐条 `requiredEvidence` 按 `evidenceLogic` 组合）。② **差异点**：`evaluateContract` **第 1 步**就查 `forbiddenEvidence` —— 任一命中 ⇒ `{success:false, confidence:0.95, forbiddenHit}`，注释明写 **"ANY match => hard fail (never override)"**（`contract.js:260`）；`requiredEvidence` 只是**第 2 步**（`:269`）。而 `businessStatePresent` **全文无 `forbidden` 概念**。③ **实测铁证（4/4）**：同一份合约 + 同一对 before/after，S1 submit（含 required 必需词 + forbidden 词）/ S2 login（profile + error）/ S3 search（result + "no results"）/ S4 logout（login + logout）⇒ A 全判 `success=false` + `forbiddenHit`，C 全判「期望业务结果**其实已达成**」⇒ **4b `VERIFICATION_TOO_STRICT` + `RETRY_VERIFY`** —— 把「页面出现错误信号」的**真实失败**说成「验证规则太严」（成功率**虚高** + repair 空转），危害与 C131/C132 同向。④ **面基数**：`Derivable` 10 个动作类型中 **4 个带 `forbiddenEvidence`**（`login`/`logout`/`search`/`submit`）= 业务主干 ⇒ **100% 落在分歧面内**，非边角。⑤ **权威依据由代码自身声明**：`verificationWindow.js:47` 注释「完整契约（含 requiredEvidence / stateType）直接透传，走 `evaluateContract`（**含 forbidden / AND-OR 语义**）」；`contract.js:5-6` 自述 *"The single source of truth for 'did the BUSINESS outcome complete'"*。⑥ **修复**：`businessStatePresent` 改为**委托** `contractLib.evaluateContract(contract, after, before, clauseVerify)` ★本批唯一生产改动（1 文件 2 处，`+33/-7`）；**口径保持 3 条**：`clauseVerify` 仍注入**本文件的 `clausePresent`**（保留诊断层逐子句第二意见，**不改用** `verify` 全量 switch 以免引入新口径差）、`element_present` 沿用 `requireActionable:false`（与 C124/C105 M3 同口径）、4b 三元式与 `verification.js`/`contract.js`/`clause.js` **一字未改**；该函数**未导出** ⇒ 不构成对外 API 变更。**方向 = 收紧**（守护 B 组反向探针证明**不是**「永不报 TOO_STRICT」的降级：required 满足且无 forbidden 时**仍报** TOO_STRICT）。⑦ **L16 形状真实性核查（诚实声明）**：`normalizeContract` 的 `filter(c => c && c.type)` 对生产契约**无实际作用**（派生契约 requiredEvidence **26/26 子句全带 type**）、`allowedAlternatives` 递归 OR **不可达**（派生契约该字段**恒为 0**）⇒ 对生产是**等价替换**；畸形契约下**两层同答**（A 也判 true，而 A=true 时 VIL 根本不被调用）⇒ 放宽路径**生产不可达**，已写成守护 D4。⑧ **L14 两 switch 差集自动对账**：`verify`(12 case) vs `clausePresent`(11 case) **唯一差集 = `action_success`**（方向保守 default false），已由 C125 守护 `REGISTERED_GAPS` 登记，本批**不重复**。⑨ **★no-op 双向**：回退 VIL（`git show HEAD:` + `cp`，sha256 `ca06aa00…` **逐字一致**）⇒ 守护 **19/28**（9 红全部**命中预期** A3/A4/C1/C2/C3/C5/D4/E4/E5）、探针回 **4/4**；恢复后 sha256 `30dc458c…` **逐字一致** + 28/28。★**额外发现**：回退态 **D4 也红** ⇒ 畸形契约（生产不可达）在改前是 A=true / C=STATE_UNKNOWN 的**第三处分歧**，本次收口一并消除（如实登记为收口附带收益）。⑩ **跨批形态**：`businessStatePresent` 是**最后一个未收口的契约评估消费方** —— 至此子句 → 组合 → 求值 → 四消费方四条链路**全部指向唯一实现**（C124 元素 → C125 子句 → C126/C127 文本 → C130/C131 url_contains → C132 `expectedActuallyPresent` → **C133 契约求值**）。
- **C132（4b `targetPresent` **两支**口径对账 —— C131 只修了一支；1×A + 1×L15 + 1×L16 死分支登记 + 1×归因更正）**：① **结构事实**：`_analyze` 4b 是**一个变量、两条分支** —— `const targetPresent = effV.businessState ? businessStatePresent(effV.businessState, after, before) : expectedActuallyPresent(effV, after);`，两支进的是**同一条** `if (targetPresent)`、**同一个** `VERIFICATION_TOO_STRICT` 返回 ⇒ 语义相同则口径必须唯一（L15）。② **走哪一支只由「planner 是否给出 verification」决定**：`buildEffectiveVerification` step 3 `if (hasRealPlannerVerif) return v;` 返回**裸 v（无 businessState）** ⇒ 走 P2 支；而 `navigate ∈ DERIVABLE` 且 `isKeyBusiness('navigate') === false`（实测）⇒ **两条支路都真实可达**（同一 navigate 动作，planner 给 `{type:'url_contains'}` ⇒ 裸 v；不给 ⇒ `{businessState:NAVIGATED}`）。③ **实测铁证**：场景 `before.url = after.url = targetUrl`（进入目标页但**无真实跳转**）⇒ P1 支 `STATE_UNKNOWN/RECHECK_OBSERVATION` vs P2 支 `VERIFICATION_TOO_STRICT/RETRY_VERIFY` ⇒ **同一变量、两种形状、相反诊断**；危害与 C131 同向 —— 把**真实失败**说成「其实已达成」（成功率**虚高**）。④ **修复**：`expectedActuallyPresent` 补第三参 `beforeObservation`，url_contains 分支**委托** `clause.evalUrlContains`（唯一实现）★本批唯一行为变更；同函数 businessState 分支补传 before（**零行为变更** —— 4b 三元式已以 `businessState` 作判别 + 本函数**未导出** ⇒ 该分支**结构上不可达**，守护 D2b 把这条判据本身写成断言）；4b 调用点传 `before`。`clause.js` / `verification.js` / `skillRouter.js` **一字未改**。**方向 = 收紧**；守护 B 组反向探针证明**不是**「永不报 TOO_STRICT」的降级（before 不含 expect 时两支**仍报** TOO_STRICT）。⑤ **跨批守护维护 5 条假红**（全部锚定**上一批的字面形状**）：C131 `D2/D2b`（原「挂起登记」）+ C130 `A3a(C2 锚点)/E4b/E6b`。归属取证：换回 `git show HEAD:` 版 VIL 复跑 ⇒ C131 **35/35**、C130 **35/35** ⇒ 确认 5 条**均由本批引入**；改锚**不变量**后 C131 **37/37**、C130 **35/35**，且**注入真违规复验仍咬得住**（非假修复）。E4b 由「字面 includes」改锚**函数体作用域**（`expectedActuallyPresent` 体内「已委托 + 无 toLowerCase」），避开全文件黑名单被同名字段误伤的过宽陷阱。⑥ **★ 归因更正（C131 的误判）**：C131 记为「phase9 被宿主回收整个进程树」——**该结论错误**。真相两条：Ⅰ `run_phase9_regression.sh` 把逐套件进度写进**它自己的** `.benchmark/phase9_regression_<ts>.txt`，**外层重定向只在结束时**输出汇总 ⇒ 「外层日志恒 1 行」**根本不构成死亡证据**；Ⅱ **Bash 工具 timeout 上限 = 600000 ms（10 min）**，传入更大的值会被**静默钳制** ⇒ 前台运行 >10 min 的回归**必然被自己杀死**。⇒ 处置：长回归改用 `run_in_background: true` 或 automation 承载；**判进度必须读脚本自己的 OUT 文件**，不读外层重定向。更正后 phase9 **正常跑完 OK=232/BAD=0**。⑦ **README 历史缺口补记**：C131 **未更新 README**（commit `84c9341` 不含 README，基线仍停 C130 的 237/0）⇒ 本批一并补齐 C131 + C132 两条历史条目与基线数字。守护 `test_c132_expected_present_parity` **28/0**（A 真实入口两支同答 / B 四条反向探针 / C 静态委托 + L17 双向咬 / D 登记面含死分支判据自证 / E 三向自检 + 生产可达性 + **全库调用点扫描**）。回归 **239/0（739.1s）** ＋ phase9 **OK=232/BAD=0**（+1 = 本批新守护）。
- **C131（`url_contains` 的 P2 无效证据守卫统一为唯一实现；1×A 跨层相反答案 + 1×L6 同文件不对称 + 1×L15）**：C130 统一了 `url_contains` 的**大小写轴**；C131 发现**同一子句的第二根分歧轴：P2 无效证据守卫的有无**。① **事实链**：该子句的判定实现全库取齐**三处** —— `verification.js:69` 裸 `url.includes(String(expect))` **＋ P2 守卫**（成功裁决面）；`skill/skillRouter.js:178` 的 `clauseVerdict(clause, obs)` **无 before** ⇒ 结构上不 P2（**设计正确**：它回答「当前观察是否匹配**前置态**」，不是因果归因）；`verificationIntelligence.js:248`（`clausePresent`）**收下了 `before` 却内联裸 includes、忽略 before** ⇒ 同一 clause、同一 before/after，**A 判 `false`/`invalidEvidence=precondition_true` 而 C 判 `true`** ⇒ 4b 误报 `VERIFICATION_TOO_STRICT` + `RETRY_VERIFY`，把**真实失败**说成「其实已达成」（跨层相反答案）。② **★ 决定性铁证**：`clause.js:136` 注释原文「P2 无效证据守卫与 `url_contains` **同理**」—— `url_pattern` 的 P2 守卫**以其与 `url_contains` 同语义为依据**，而 `url_contains` 恰恰**没有** ⇒ **注释声明的语义与实现脱节**（L16 谱系：参照物不存在）。契约依据 `planner.js:79/86`：「expect 必须是**动作执行前 URL 中不存在的片段** —— 若入口 URL 已包含该片段，**验证将被判为无效证据而失败**」⇒ P2 是**子句语义的一部分**，不是局部加固。③ **生产可达**：`contract.deriveContract({type:'navigate', target:{url}})`（★签名是 **action 对象**，传字符串返回 `null`）产出 `requiredEvidence:[{type:'url_contains', expect}]` → `businessStatePresent` → `clausePresent`（正是缺 P2 的分支）。④ **修复**：`clause.js` 新增 **`evalUrlContains(cl, after, before)` = `url_contains` 唯一实现**（P2 表面判定 host+pathname、**query 不参与**；无 `before.url` 不触发 P2；缺 expect fail-closed；大小写两侧**原样**承 C130）；`verification.js` 与 VIL `clausePresent` 改为**委托**；删除已成死引用的 `const { urlSurfaceKey, surfaceContains } = clause;`。**方向 = 收紧**。⑤ **★★ 改共享实现会扫红旧守护（本批最重要教训）**：C125 **71/0→70/1**、C130 **34/35→31/35**，共 **5 条假红** —— 旧守护断言的是「**上一批的写法**」而非**不变量**。归属判定：`git show HEAD:` 换回原件复跑 ⇒ C125 恢复 71/0 ⇒ 确认 5 条**均由本批引入**。处置：C125 E6 原为**全文件** `!/v\.expect/`，被新子句的**同名字段** expect 误伤 ⇒ 收敛到 **`evalUrlPattern` 函数体作用域** + 补正向断言 ⇒ **73/0**；C130 的 A3a/A3a0/C2b/E2a/E4d 从**字面形状**改锚**不变量**（分支存在 + 已委托唯一实现）⇒ **35/35**。两条修订均**注入原目标缺陷复验仍咬得住**。⑥ **no-op 双向**：回退 VIL ⇒ **7 红**（含复现跨层相反答案）；回退 `verification.js` ⇒ E2 红；恢复后与备份 **sha256 逐字一致**。守护 `test_c131_url_p2_parity` **35/0**（A 三层真实调用 / B 双向反向探针 / C 静态委托 + 唯一实现 / D 有意分离登记 / E 三向自检 + 可达性）。回归 **238/0（762.4s）**（+1 = 本批新守护）；commit `84c9341`。
- **C130（`url_contains` 大小写口径跨层统一；1×B 收紧 + 1×L15 唯一化 + 2×守护自身缺陷修复）**：C129 在逐项核对「同一子句类型的**全部**消费方」时已把本批登记为独立批次，本批落地。① **事实链（L5 方向自检）**：`url_contains` 的判定实现全库取齐共**三处** —— `verification.js:70` 裸 `url.includes(String(expect))`（**大小写敏感**，成功裁决面）· `skill/skillRouter.js:182` `url.includes(e)`（**敏感**）· `verification/verificationIntelligence.js` 的 `expectedActuallyPresent`（`:189/:196`）与 `clausePresent`（`:228/:233`）**两侧都 `.toLowerCase()`**（**不敏感**）⇒ 多数派 = 敏感、少数派 = VIL。**四条判据全指向 VIL 是异类**：Ⅰ **RFC 3986 §6.2.2** —— scheme/host 大小写**不**敏感、**path 大小写敏感**（`/Dashboard` ≠ `/dashboard`），整体 `url.toLowerCase()` 会**同时**把 host（该折叠）与 path（不该折叠）一起折叠 ⇒ **对哪条规则都是错的**；Ⅱ `clause.urlSurfaceKey`（C125 唯一实现）已给出正确形态 `((p.hostname||'').toLowerCase()) + (p.pathname||'/')`（host 折叠 / path 原样）；Ⅲ `planner.js:86` 权威措辞「`url_contains`（**URL 变化**；expect 必须是**动作执行前 URL 中不存在的片段**）」且 **planner 全文无一处 `toLowerCase`**；Ⅳ `contract.deriveContract('navigate')` 产出 `{type:'url_contains',expect:'__URL__'}` 且 `substitute()` 用 `action.target.url` **原样替换** ⇒ 正常路径下 expect 与页面上报 url 同源同大小写。② **行为矩阵（改前实测）**：`HTTPS @ https://a.com/`、`https @ HTTPS://a.com/`、`/Dashboard @ …/dashboard`、`/dashboard @ …/Dashboard`、`/SIGNUP @ …/signup` 五例 **A=false B=false C=true**（VIL 偏宽）；诊断层因此把「仅大小写不同」误判为「业务结果其实已达成」⇒ `_analyze` 4b 误报 `VERIFICATION_TOO_STRICT` ⇒ repair 走替代态判定，而事实是**业务未达成**。③ **修复**：VIL 四处去掉 `.toLowerCase()`（改前 sha256 `ec36dda5…` → 改后 `5153f770…`，`+19 −4`）。④ **§0.4 判据（`clausePresent` 的局部 `url` 变量另有用途）逐一核对**：该变量第二消费点是 `page_change` 的 `(b.url && b.url !== url)` —— **差值比较**（非包含判定），`a !== b` 与原样比较方向**一致**，「仅大小写不同的真实跳转」（`/Dashboard` → `/dashboard`）旧实现被吞掉，去掉 lowercase 是**修正**而非新分歧。⑤ **§0.7 诚实声明**：本批**不放宽**任何判定，`TOO_STRICT` 输出**减少**；`decision`/`failureType` 取值集合、Success Definition、阈值**全不变**；**无当前产品收益承诺** —— 修的是**口径唯一性（L15）**，不是成功率。⑥ **守护** `test_c130_url_case_parity` **34/34**：A 组**三层真实调用**同答（`verification.verify` / `router.clauseVerdict` / `vil.analyze`，**非手写模拟**）＋ A2 防空断言 ＋ **A3a0/A3a/A3b/A3c 覆盖面逐锚点双向对账**；B 组三条反向探针（**不得放宽** / **不得降级** / VIL 两条独立路径同答）；C 组 RFC 3986 依据登记（`urlSurfaceKey` host 折叠＋path 保留、`surfaceContains` 裸 includes）；D 组有意分离登记（**D3 `detectAsyncPending` 的 url 仍 lowercase = 路径关键词命中语义，有意不敏感，一字不改**）；E 组连带面与放宽0/收紧4/未动三向自检。**C129 守护 C4 升级为三层**（原先是「两层」且 verification 侧是**手写模拟** `'https://a.com/'.includes('HTTPS')`，**恰好漏掉第三层 VIL** —— 而 VIL 正是唯一不敏感的实现）⇒ C129 仍 **40/40**。⑦ **★★ no-op 双向验证暴露并修掉 2 个守护自身缺陷**：**Ⅰ E6 是 `ok(true, …)` 硬编码恒真**（任何情况都绿 = **为不存在的失败写断言**）⇒ 改为从源码实测 4 条 loosening 形状 ＋ E6b 测 4 条 tight 形状；**Ⅱ A3a 只写 `shape` 正则、缺 `revert` 方向** ⇒ 回退时 `bad` 保持空 ⇒ **A3b 真空绿** ⇒ 每条锚点补 `revert`（A3a0）。另测出 **C3 的 original 形态与 D3 登记面（`:150`）逐字相同** ⇒ 不带上下文的正则在**已修复**文件上命中 :150 ⇒ **假红**，须用紧随声明（`const els =` vs `const elements =`）消歧。⑧ **no-op 实测**：改前 C130 **25/34**（A1/A3a0/B1/E4a–E4d/E6/E6b 共 9 红，**全部预期**）、C129 **39/40**（**仅** C4）；**其余 18 个干系套件 no-op 下全绿** ⇒ 本修复的行为面爆炸半径**实测** = 仅「诊断归因层」，未触及成功裁决面 / 文本通道 / 字段通道。还原后 C130 34/34、C129 40/40、C128 45/45，`sha256sum -c` 保真、`NO-OP` 残留 0。⑨ **三层探针**：改前 `DIFF_COUNT=6` → 改后 **1**（唯一残差 = `a.com` 既有 P2 恒真守卫面，非本批引入）。回归 **237/0（841.7s）+ OK=229/BAD=0**（计数增量 +1 = 本批新守护）。
- **C129（`text_present` 跨层 expect 归一化口径对账；1×B 收紧 + 1 边界关闭 + 1×L5 方向自检）**：C128 把「`skillRouter.clauseVerdict` 的 expect 归一化口径与 `clause.normalizeText` 分歧」如实登记为**边界**并加守护钉住（C10），本批把它修掉。① **事实链**：同一个 `text_present` 子句的两个消费方各用一套归一化族 —— `server/agent/skill/skillRouter.js` 的该分支对**两侧**都调 `norm()`（把标点折叠成空格），而 `server/agent/verification/clause.js` 的 `evalTextPresent` 走 `pageText(obs)`（判定口径，**保留标点**）＋ `existence.normalizeText`（只折叠空白+小写）⇒ `expect='a.b'` / 页面文本 `'a b'` 时 **skill=TRUE 而 clause=FALSE**。no-op 轮的 8 例标点敏感矩阵实测**改前 7 例**都是这个相反答案（英文句点/感叹号、中文全角句号/全角括号、版本号中的点）⇒ **不是单点而是整类**（C128 只登记了 `a.b` 一例，低估了分歧面）。② **谁是异类（L5 方向自检）**：`expect` 是**页面文本片段**，比较对象是 `pageText` —— C126/C127 已把「页面文本」收口为唯一口径，clause 侧正是事实源口径 ⇒ **skill 侧的 `norm` 是用错地方的口径**（`norm` 在 `elementTokens`/`elementMatchesField` 的**词法匹配**语义下才是对的）⇒ 修调用方，**不**动被调方。③ **修复**：该分支文本侧直接用 `pageText(obs)`（不再二次归一化）、expect 侧改为 `normalizeText(clause.expect)`（与 `clause.js` **同一模块导入 = 同一函数引用**，不是第二份同义实现，L6）；判定**策略**零变化（三态：文本为空 ⇒ `INDETERMINATE`）；`norm` 在该文件其余调用点原样保留。**方向 = 收紧**（skill 侧原先更宽：抹掉歧义字符后更易误判 TRUE）。④ **风险等级证明（判据而非声称）**：`skillRouter.gateOf` 要求 `status==='ACTIVE'`，而 `skillBuilder` 恒产 `CANDIDATE` ⇒ `eligible` 恒空 ⇒ `decision` 恒 `GENERIC`、prestate 走 `SHADOW` ⇒ **本批在生产是行为中性的**（只改影子预检观测值，不影响决策/执行/成功裁决）。仍要修的理由：SHADOW 是 17-D **晋升判定**的输入证据 ⇒ **诚实登记：本批无当前产品收益**。⑤ **C128 C10 升级（不弱化）**：从「边界仍存在」哨兵改为「两层同答」＋**双向反向探针**（不得放宽 ⇒ 不得把 FALSE 翻成 TRUE；不得降级 ⇒ 不得把确定判定降为 `INDETERMINATE`）。⑥ **★ 本轮额外发现（第二处跨层分歧，登记为 C130 独立批次）**：逐项核对「同一子句类型的**全部**消费方」时发现 `text_present` 不是唯一分歧面 —— `url_contains` 在 `verification.js` 与 `skill/skillRouter.js` 用 `url.includes(...)`（**大小写敏感**），而 `verification/verificationIntelligence.js` 把两侧 `.toLowerCase()`（**不敏感**）⇒ 同一条子句两层相反答案。按 RFC 3986 **path 大小写敏感**（`/Dashboard` ≠ `/dashboard`）⇒ VIL 侧 lowercase 是缺陷；且 `url_contains` 是 `deriveContract('navigate')` 的**权威业务契约**（主链路）⇒ VIL 会误判「已达成 ⇒ TOO_STRICT」，把真实失败错误归因。**因双回归在途、改动面已冻结，不混入第二处修复**（避归因混淆）⇒ 本批只登记；C129 守护的 C4 对账当前覆盖两层，修 VIL 时须升级为**三层对账**。守护 `test_c129_clause_parity` **40/0**（A 覆盖面自动对账 14：含**分支集合 vs `skillSchema.OBSERVABLE_TYPES` 自动对账**——新增子句类型未处理即红——＋**静态委托断言 `normalizeText` 同一函数引用** / B 跨层一致性行为矩阵 8 / C **有意分离登记** 10（`field_value` 全等 vs 子串、`url_pattern` 字段与语义、`element_present` 机制 —— 无声改变即红）/ D 三向自检 8（`decision` 面 / SHADOW 面 / CAPTCHA 面未动））；**no-op 双向验证**：临时还原为 `norm` 版 ⇒ 预测 `{A4a,A4b,A4c,B1,B2,D5}` ＋ C128 `{C10}` ⇒ 实测 C129 **34/40**、C128 **44/45**、其余 12 套件 **12/12 rc=0** —— **精确命中**，恢复后 `NO-OP-ROUND` 残留 0、C129 40/40、C128 45/45；干系套件改前 13/13 → 改后 14/14 全 rc=0。**未动 6 类**（`verification.js` 成功裁决面 / `botChallenge` CAPTCHA 面 / `contextBuilder`·`planner`·`deepseek` prompt 预算面 / `existence` 的 strict-loose 双档 / `skillRouter` 五级判定与决胜规则 / `pageText` 本身）。回归 **236/0（805.5s）+ OK=229/BAD=0** 双支零红项（+1 = 本批新增守护）。
- **C128（为不存在的形状写代码·第三副面孔 × 页面文本口径剩余消费方收敛；1×A + 3×B + 1×L16 盲区）**：C127 登记的 L16 第三副面孔在本批落地为**真实缺陷**。① **A1（A 类/死兜底）**：`contextGuard.js` 的 `resolveCapabilities` 在 pageState 无能力标签时按注释承诺「观察可得时现算兜底」，但它把**原始 observation** 交给了 `pageStateClassifier.detectCapabilities` —— 后者的入参契约是 `extractText(obs)` 的**抽取形状** `{text,url,elements}`，而 `observation.js:64` 的产出表里**没有 `text` 字段**（只有 `textSummary`/`visibleText`）⇒ `const {text} = obs` 得 `undefined` ⇒ `text.toLowerCase()` 抛 TypeError ⇒ 被 `catch (e) { /* 兜底失败即视为无能力证据 */ }` **整段吞掉** ⇒ `return []`。**该兜底分支在生产上从未成功执行过一次**，而它恰是 C127（`pageStateClassifier` 读全量文本）所依赖的通路 ⇒ 长页面尾部能力信号（支付/下载/空结果）在 `contextGuard` 侧**仍然漏检**。② **谁是异类（L5 方向自检）**：`detectCapabilities` 全库只有两个调用点 —— `detect()` 内部传 `extractText(obs)`（正确）、`contextGuard` 传原始观测（错）⇒ 契约即抽取形状，**修调用方**；**不**把被调方改成「两种形状都吃」（那会把调用方缺陷永久藏起来，正是 L16 的成因）。③ 修复 = Ⅰ**显式形状契约**（`assertExtractedShape` + `code='E_CAPABILITY_SHAPE'`，错误文案自描述「原始 observation 正是这种形状」）＋Ⅱ兜底异常**仍 fail-open**（不放宽也不收紧守卫规则 5 的闸门）但**必须留痕**（`MAX_FALLBACK_ERRORS=20` 有界环形 + `capabilityFallbackErrors()`/`resetCapabilityFallbackErrors()`）＋Ⅲ`contextGuard` 改传 `classifier.extractText(obs)`。④ **行为中性证明（判据而非声称）**：生产唯一调用点 `tools.js:209-213` 的 `pageState` 由 `classify(同一观测)` 产出 ⇒ 兜底现算的输入与 `classify` 的输入**完全相同** ⇒ 不变量「兜底现算 ≡ `classify(obs).capabilities`」⇒ `ps.capabilities` 为空时现算**也必为空** ⇒ 仍 fail-open ⇒ **生产行为中性**；唯一收紧面 = 手工构造「无 capabilities 的 pageState + 传观测」（本仓库只有测试）。⑤ **B 组三处页面文本口径收敛**（C126/C127 口径唯一化的剩余消费方）：`diagnosisEngine`（直读 `textSummary`）、`failureDiagnoser`（`obs.textSummary || obs.visibleText || ''`）、`skillRouter`（**自建同义拼串**）⇒ 一律走 `pageText.js` 唯一口径、各层只留调用点；三个判定消费方的正则**全带 `/i`** ⇒ 口径从「前 120 元素」放宽到全文**不改任何公式**。⑥ **方向自检**：**放宽 3 条**（页面文本可见性 / 能力标签可见性 / 诊断证据文案取样面）、**收紧 1 条**（兜底不再静默 + 形状契约 fail-loud）、**未动 6 类**（`verification.js` 成功裁决面、`botChallenge` CAPTCHA 面、`contextBuilder`/`planner`/`deepseek` prompt 预算面、`skillRouter.clauseVerdict` 的 expect 归一化口径 —— 与 `clause.normalizeText` 的标点折叠分歧已登记为守护，本批不动）。守护 `test_c128_context_shape_parity` **45/0**（A 形状/字段名真实性 10 —— 含**全库调用点扫描**：任何 `detectCapabilities(` 实参不含 `extractText(` 即红，并带「扫到 >100 文件」防空断言；B 行为探针 14 —— 含 **B2 死兜底探针**（`{state:'PAYMENT'}` + 支付观测 ⇒ capabilities 必须含 payment，改前恒 `[]`）、**B6a–c 恶意 Proxy 端到端留痕**、B9 声明性收紧；C 口径唯一 16；D 三向自检 5）。**no-op 双向验证两轮**：Ⅰ「形状契约 + 调用点」家族 ⇒ 预期 `{A3,A4b,B2,B9}` **实测 4/4**；Ⅱ「口径收敛 + 留痕」家族 ⇒ 预期 11 项 **实测 11/11**；还原后 `sha256sum -c` **6/6 OK**、无 `NO-OP` 残留。★ **诚实登记**：B3「正确性不变量」（兜底现算 ≡ `classify`）**咬不住 A1** —— 改前两侧走同一条错误路径同样得 `[]`；能分辨的只有 B2 与 B6。★ **本批最贵的认知「正确性 ≠ 有效性」**：`catch` 的 fail-open 返回值是**空集**，而**空集恰是下游合法输入**（守卫规则 5 的 fail-open 闸门）⇒ **缺陷与正常态同形**，无痕、不报错、无告警 ⇒ 唯一正解 = 显式形状契约 ＋ 有界留痕 ＋「输入非空必须得非空」的正向探针，三者缺一不可。⑦ **同批纪律事故（C6 第二次复现）**：同一批发出的两个 Edit，后者按**旧快照**覆写、静默丢弃前者的 require 行，**工具仍返回 success**（守护立刻咬出）⇒ 新增可执行动作「**每文件每批只发一个 Edit，改完立刻回读/核 sha256**」。回归 **235/0（872.0s）+ OK=228/BAD=0**。
- **C127（页面文本通道字段名真实性 × 唯一口径；3×A + 1×L2 盲区）**：C126 把「页面文本」收口到 `clause.js` 的 `pageText`，但**同一份文本还有另外两个消费方**在真实链路上**永远读不到它**。① **A1**：`recovery/failureSnapshot.js:31` 写 `Array.isArray(observation.textSummary) ? observation.textSummary : [observation.textSummary.slice(0,2000)]` —— `textSummary` 是**字符串**（`observation.js:486`），`Array.isArray` **恒 false** ⇒ 第一分支**永不可达** ⇒ 名为 `visibleTexts`（= 全页可见文本行）的字段实际只装「前 2000 字符」；两个下游（`diagnosisPrompt.js:37` 按行 `slice(0,20).join(' · ')` 给 LLM、`executionFailureTaxonomy.js:63` 做 CAPTCHA 弱信号）都按全页消费 ⇒ prompt 只剩**一整块 1500 字符**（1 行而非 20 行）、窗口外的挑战控件**静默漏检**。② **A2/A3**：`pageReady.js:17` 与 `pageStateClassifier.js:70` 都优先读 **`visibleTexts`（复数）**，而 `observation.js` **从不产出复数名**（只有 `visibleText` 单数）⇒ 分支恒不可达、恒静默回落窄口径 `textSummary`；`pageStateClassifier` 在**真实执行链路**上（`tools.js:209` → `contextGuard` 动作前提校验）⇒ 长页面尾部能力信号（支付/下载/空结果）**能力标签漏检**、纯文本页被误判 `BLANK(0.95)`。③ **★ L2 盲区的新面孔**：这两个「生产不存在的形状」**只活在测试 fixture 里**（`test_phase6.js` 13 个 fixture 全用 `{visibleTexts:[...]}`；`testAgentPhase22.js:95` 用 `textSummary:[...]`）⇒ 守护断言的是**幽灵路径**，234 个套件全绿而生产带伤（既非 C124 的环境塌缩，也非 C122 的「只走一次」）。④ **L5 方向自检**：横向清点 `skillRouter.js:174` / `repair/strategies/verifyFailed.js:40` / `failureDiagnoser.js:346` 早已读全量 ⇒ 读窄口径/读幽灵字段名的都是少数派。修复 = 新建中立模块 **`server/agent/pageText.js`** 作为唯一通道（`pageText` = 判定口径：拼接 + 折叠空白 + 小写；`pageTextLines` = 取证口径：**保留原始大小写**、逐来源片段拆行、去重、限量；`roleText` 不进通道）；`clause.js` 改为**再导出同一函数引用**（`clause.pageText === pageText.pageText`），`pageReady` / `pageStateClassifier` / `failureSnapshot` 只留调用点，BLANK 信号文案里的幽灵字段名一并改掉；`failureSnapshot` 落库上限显式化为 4000 字符。**方向自检**：放宽的只有「页面文本可见性」；**收紧**的是落库体积上限与「不再为不存在的形状写分支」；未改任何阈值 / success 定义 / 验证成功裁决。守护 `test_c127_text_channel_parity` **45/0**，**A 组（字段名真实性）= 本批最高价值**：解析 `observation.js` 的 `out.<field>=` 产出集合，断言「必须产出 textSummary/visibleText、**不得**产出 visibleTexts」+ 三个消费方不得再出现 `visibleTexts` + 不得再为 `Array.isArray(textSummary)` 写分支 + **fixture 自身**不得再用数组型 textSummary。**no-op 双向验证两轮、两个互补家族**：① 宽窄家族（`collectParts` 退回只读 `textSummary`）⇒ 红 10 条全落目标、**A 组 13 条全绿**（证明 A 与 B 正交）、且**连 C126 一起红 7 条**（证明 `clause` 是真委托不是假再导出）；② 结构性家族（恢复死分支 / 恢复复数名读取）⇒ 分别**恰好红 A8+D7 / 恰好红 A4**，行为断言全绿（证明字段名断言隔离有效）；每轮还原 `sha256sum -c` **8/8 OK**。**相邻守护锚点上移（L3 不删不降）**：`test_c126` A1 由「clause.js 必须有 `function pageText`」上移为「clause.js 必须委托 + pageText.js 必须有实现」并**新增**同引用断言，A8 统计范围扩为两文件（**47/0 → 49/0**）；`testAgentPhase22` fixture 由数组型 `textSummary` 对齐为生产形状、断言由「是数组」**加强**为「确实是页面文本行」。**本批修出的自己的真 bug 如实登记**：`pageTextLines` 首版误写 `collect(o)`（opts 而非 obs）⇒ 单段超长文本**返回空数组**（静默丢文本），已修并固化为 E6 断言。**登记未修**：`contextGuard.js:144` 把**原始 observation** 传给期望 `{text,url,elements}` 的 `detectCapabilities` ⇒ `text` 为 undefined ⇒ 抛 TypeError 被 try/catch 吞 ⇒ **兜底恒 `[]`**（当前不可观测 = death fallback，另批）；`diagnosisEngine:15` / `botChallenge:71` 仍读窄口径（语义不同，C126 已登记）；`textSummary` 采集容量未动。回归 **234/0（1237.4s）+ OK=227/BAD=0（22m37s）**。
- **C126（页面文本证据口径统一；1×A + 1×B + 1×证据层级）**：`observation.js` 产出两个容量与构造都不同的文本字段 —— `textSummary` = **前 120 个筛选元素**的文本（空格连接，截断 5000）、`visibleText` = 全页 `body.innerText` 去重行（换行连接，截断 8000）。**三个消费方三种口径**：验证引擎 `text_present`/`text_absent` 只读 `textSummary`（最窄）、诊断层 `expectedActuallyPresent` 读 `visibleText || textSummary`、诊断层 `clausePresent` 再叠加 `roleText`。① **D1（A）**：长页面上目标文本落在第 121 个元素（或 5000 字符窗口）之后时 `text_present` **恒假** ⇒ 真实业务成功被判验证失败，且诊断层给出**相反答案**（判 TOO_STRICT）——与 C123/C124 同族事故第三次出现。横向清点（L5）反证了修复方向：`skill/skillRouter.js:174` 早已用 `[textSummary, visibleText].join(' ')`、`repair/strategies/verifyFailed.js:40` 用 `visibleText || textSummary`、`diagnosis/failureDiagnoser.js:346` 用两者兜底 ⇒ **验证引擎是唯一读窄口径的那个**。② **D2（B）**：`text_absent` 与 `text_present` 本会各持一份口径 ⇒ 「present 说在、absent 也说不在」的形式矛盾（L6）。③ **D3（证据层级）**：诊断层把 `roleText`（交互元素角色名 / `aria-label`）混进文本通道 ⇒ 视觉上没有这段文字却命中 `text_present`（假阳性来源），而验证引擎从不读它（又一处跨层分歧）。修复 = 在 `clause.js`（C125 建立的唯一实现）新增 `pageText`（`textSummary + visibleText` 拼接后归一化）与 `evalTextPresent`/`evalTextAbsent`，两侧只保留调用点；`roleText` 退出文本通道但保留在 `page_change` 的两侧构造里（C124 D5 要求比较型谓词两侧同口径）。**方向自检**：唯一放宽的是 `text_present`（从「看不见页面一半文本」到「看得见全部可见文本」，这正是它的定义）；`text_absent` 与 `login_state` 均**收紧**（更不易误判「已消失」/ 更不易伪造登录成功）。**登记未修 3 条**：`diagnosisEngine`/`botChallenge`/`pageReady` 三处仍读窄口径（属关键词扫描/挑战识别/就绪判定等不同语义）；`observation.errors` 的关键词扫描仍用 `textSummary`（改动会连带改变 `forbiddenEvidence` 的输入面）；`textSummary` 的 120 元素 / 5000 字符采集容量本身未动（本批修的是消费侧可见性，不是采集侧容量）。守护 `test_c126_text_source_parity` **47/0**（口径唯一性 8 + 长页恒假修复 3 + 跨层 parity 21 + 证据层级 4 + fail-closed 8 + 防空 3）；no-op 双向验证红 **7 条**全落目标（含「两层同时错」—— 精确刻画本批修的是**口径宽窄**，跨层一致性由静态委托断言守），还原 sha256 三文件全 `SAME`。回归 **233/0（1224.6s，其中 step22 单套件 682.1s 为耗时主导）+ OK=226/BAD=0**。
- **C125（业务子句覆盖面跨层对账；3×A + 1×B）**：C124 之后的横向复审（L5）追问：同一份子句语义的两个 switch，**分支集合**是否一致？不一致——验证引擎 12 个分支，诊断层 `clausePresent` 只有 9 个，**`storage` / `url_pattern` / `login_state` 落 `default: return false`**。① **D1/D2**：这三类子句在诊断层恒假 ⇒ `businessStatePresent` 永远为假 ⇒ `_analyze` 的 4b「业务结果其实已达成 ⇒ TOO_STRICT 给 repair 机会」走不到 ⇒ 真实成功被 4c 判「证据不足」**升级人工**（HUMAN_ESCALATION 上升）；`AND` 逻辑下契约只要含其中任一条就恒成立不了。主链路实证：`skillSchema.OBSERVABLE_TYPES` 含 `url_pattern`、`skillBuilder.js:284/369` 直接产出它、planner 契约文本明确引导 LLM 产出三类子句。② **D3（同文件内不对称）**：`_analyze` 4c 专门为 `login_state` 写了「脆弱正则 ⇒ TOO_STRICT」兜底，但该兜底只覆盖 `expectedVerification.type` 这一条路径，`businessState.requiredEvidence` 里的 `login_state` 到不了 4c。③ **D4**：按最小改法在诊断层补分支会让 storage 取值/正则 fail-closed/P2 守卫再抄一遍 = 第三份同义实现（L6）。修复 = 抽 **`server/agent/verification/clause.js`** 作为这三类子句判定的唯一实现（置信度随判定一并返回；`url_pattern` 传入 before 使 P2 恒真守卫与验证引擎同口径），两层只保留调用点，逐例核对置信度与 fail-closed 语义零变化。**登记未修的边界 5 条**：skill 层 `url_pattern` 用 `expect` 字段且只匹配 path（默认模式 `'/'`，若顺手兼容会让正则 `/` 在任意 url 上命中 ⇒ 把恒假翻成**恒真**，故**有意不兼容**并加守护钉住）；`expectedActuallyPresent` 是宽松语义、与 `clausePresent` 分工不同不合并；`action_success` 在诊断层有意不覆盖（动作已成功时恒真，覆盖会把一切失败解释成验证过严）；文本口径跨层不同（`textSummary` 取前 120 个文本节点 vs `visibleText` 全量）需单独归因；`skillRouter.clauseVerdict` 是三值 path 匹配，语义有意分离。守护 `test_c125_clause_coverage` **71/0**（跨层 parity 33 + P2 守卫 4 + fail-closed 9 + **覆盖面自动对账 8** + 唯一实现 7 + 主链路实证 6 + 防空 4）；no-op 双向验证红 **8 条**且全部落在目标（含 3 条 `vil=false engine=true` 的跨层矛盾实锤），还原后 sha256 保真。回归 **232/0（711.5s）+ OK=225/BAD=0**。
- **C124（存在性裁决跨层一致性；3×A + 1×B，含 1 条恒真子句）**：横向复审（L5/L11）发现：同一份「存在性语义」在**裁决层**（`verification.js`）与**诊断层**（`verificationIntelligence.js`）是两份各自实现，对同一份观察给出相反答案。① **D1**：`clausePresent` 的 switch **没有 `element_present` 分支** ⇒ 落到 `default: return false` ⇒ 任何 `requiredEvidence` 含 `element_present` 的业务契约在 VIL 里结构性不可能成立——而 `contract.deriveContract({type:'search'})` 直接产出 `{type:'element_present',expect:'results'}`，planner 契约文本也要求登录态用 `element_present` 证明。② **D2**：`element_absent` 走裸 `semanticResolver`（无 `contentLeaves` 索引）⇒ 目标以 `div/span` 形态**仍在页面上**却判「已消失」⇒ 谎报业务结果达成（**伪造 Business Success 信号**；典型＝click 没生效、页面没动，VIL 却报「结果其实达成了」）。③ **D5（本批最重）**：`page_change` 子句的 after 侧恒拼 `' ' + roleText`、before 侧不拼 ⇒ 两侧字符串**永不可能相等** ⇒ 有 before 观察时该子句**恒真** ⇒ `deriveContract({type:'click'})` = OR(page_change, element_absent) 恒真 ⇒ 所有关键 click 步骤的 VIL 归因失去区分力。实证：同一组完全相同的 before/after，`verification.verify({type:'page_change'})` 判 `false`（正确）而 VIL 判 `TOO_STRICT`。④ **D4**：存在性通道施加了可操作性否决（违反 C105 M3）。修复 = 抽 **`server/agent/existence.js`** 作为存在性裁决的**唯一实现**（strict / loose 两档由调用方显式选择，loose 弱信号绝不进成功裁决），两个消费层改为委托；`page_change` 两侧改为同一构造函数 + 同一归一化。**诚实记录两个「不成立」的假设**：旧口径的跨元素字段拼接**不会**造成假阳性（元素间接缝恒含 ≥6 个空白，现实不可达，故不声称修它）；`detectAsyncPending` 的口径差异只做关键词包含、不比较两侧 ⇒ **非缺陷**。守护 `test_c124_existence_parity` **36/0**（真实 `analyze` 决策级探针 4 + no-op 双向 2 + 跨层 parity 5 + strict/loose 隔离 6 + 静态契约 6 + 防空 3 + **真实 `deriveContract` 推导契约** 4 + page_change 同口径 5）；`test_c123` E2 锚点上移为「全仓唯一 + 两处委托」（不弱化）。回归 **231/0（784.6s）+ OK=224/BAD=0**。
- **C123（element_present 存在性索引缺口——P1 六例根因；1×A 验证通道假阴性）**：`observation.elements[]` 是 `semanticResolver` 的**唯一**候选池，而采集层**刻意不收 `div/span/p`**（`observation.js` 容量取舍：数量大、会挤爆 80 条上限并稀释排序）。该取舍的书面理由是「文本已由 textSummary 覆盖，text_present 验证不受影响」——**只覆盖了 text_present，遗漏了 `element_present` 同为这个池的消费者**。实证：v2 基线 100 任务中 **99 个首动作 navigate、其中 91 个（92%）用 `element_present` 验证**；`scraping/list.html` 上 `textSummary` 完整含目标条目而 `elements=0` ⇒ `text_present=true` 但 `element_present=false`（同一事实两条通道结论相反）⇒ 4 次验证全败 ⇒ HEALING ⇒ 重试耗尽 ⇒ HUMAN_ESCALATION（P1 六例中的 5 例）。修复 = 另建 `contentLeaves` **存在性索引**（有直接文本 + 带 class/id + ≤120 字符 + 全页 ≤40 条），**不进 `elements[]`**、**不进 `contextBuilder`（零 LLM token 成本）**，`element_present` 与 `element_absent` **共用同一回落**（避免「present 说在、absent 也说不在」的矛盾）。★ 否决方案：直接把 span 塞进 `elements[]` 会绕过护栏——`DESCRIPTIVE_TAGS` 不含 span ⇒ 判为 `passive` ⇒ 不享受 `DESCRIPTIVE_CEILING` 封顶且排序优先于 descriptive，会带偏 click/fill 目标。守护 `test_c123_content_leaf_existence` **23/0**（真实 Chromium：命中/判别力/不破坏既有池/present-absent 对称/静态契约/防空断言六组）；no-op 红 7 项精确咬住。**回归前被既有守护抓出 3 处问题（含 1 个我引入的真实 bug：COLLECT_JS 是模板字符串，`/\s+/g` 被解析为 `/s+/g`）：`c94` 裸动态端口 + 注释含被禁字面量、`phase6` 断言过时（L3 上移锚点）、`step5` 转义与品牌词**。回归 **230/0（780.7s）+ OK=223/BAD=0**。
- **C122（runtime.run() 异常结算作用域缺陷；1×A 取证链断裂 + 守护层 L2 盲区）**：PHASE 17-E 把 `skillSession` 声明在 `run()` 的 **try 块内**，却在 `catch (fatal)` 里引用它做异常结算——JS `let` 块级作用域下 **catch 看不到 try 内的声明** ⇒ 任何未预期异常进入 catch 时 `if (skillSession)` 自身抛 `ReferenceError: skillSession is not defined`：① 17-E 异常结算被跳过；② **原始异常整体丢失**（Phase 5.8「未预期异常 → 显式 FAILED + 保留原因」防御被彻底架空）。发现路径 = P1 勘查定向 smoke（`--ids` 6 任务 + 隔离 `FPB_DATA_DIR`）：6/6 在 3.4s 内 FAILED 且 error 一律为该 ReferenceError。**为什么 229+222 全绿却主链路带伤**：缺陷只在「未预期异常」路径触发（正常路径永不进 catch）；既有守护 `test_c113:703` 只做文本断言（`/skillSession\.finish\(/` 存在），对作用域语义零咬合力；无任何套件端到端执行 fatal 路径 ⇒ **全绿守护护不住「只走一次的路径」**（L2 盲区在守护层复现）。修复 = 声明提升到函数作用域（try 之前）+ try 内禁止重复声明；守护 `test_c122_runtime_catch_scope` **13/0** 三层：A 静态作用域（stripComments 后声明唯一/位置/catch 引用而非重声明）+ B vm 微复现 bug 类（try 内声明+catch 引用必炸/函数作用域必好）+ C **真实模块行为探针**（假 session 零浏览器 + planner 抛 `PROBE_FATAL_CAUSE_XYZ` ⇒ 断言不外抛 + FAILED + error 保留原始原因）；no-op 对照红项 = A1/A1b+C1/C2/C3 精确咬住，sha256 还原保真。**修复价值实时实证**：修复后同 smoke 重跑，6/6 error 从 ReferenceError 变为 `规划失败(plan): LLM HTTP 402: Insufficient Balance` —— 被吞的真因（DeepSeek 余额耗尽，外部条件）完整浮出。**P1（element_present 就绪收敛）复现被 402 外部阻断，余额恢复后一步可续**（入口见 C122 报告 §7）。回归：runRegression **229/0** 全绿（781.1s）+ phase9 **OK=222/BAD=0**。
- **C121（文档数值声明 × 事实源 自动化守护；兑现 C120 登记未做项）**：C120 修了事实（README 16-B patch 计数 6→10）但登记「未建自动化守护」。漂移根因 = **单调递增字段（`enabled` 只 false→true）× 人工快照 × 翻真发生在逐批协议外的 16-B 外部窗口 = 必然静默漂移**。守护 `test_c121_doc_fact_consistency` **22/0**：A 提取器自洽（README 与 manifest 双解析零硬编码）/ B 硬不变量（README 声明数 === `enabled:true` 数，`isPatchActive` 模块行为双重 grounding）/ C 双向覆盖（每个 ACTIVE patch 在 README 有 verbatim 标识符 + 每个 README 标识符映射回 patch，防过期名残留）/ D 事实源指针 + E 防空 + F 五探针（计数漂移/新 patch 翻真/标识符缺失/指针断链/no-op）/ G stripComments + CRLF。★ 锚点上移（L3）：README 16-B 列表从人写别名升级为 verbatim patchId/surface。★ 边界登记（L9）：套件计数/OK 计数是**自引用量**不在守护范围（守护自身入库即 +1，等值断言会自红）；`enabled` 是协议外翻真的无人守护面，正是 C120 事故地。
- **C120（文档数值声明 × 事实源 一致性：README 16-B patch 计数纠偏；1×B 文档漂移）**：README「Native 身份架构（16-B）」能力行声明 **6 个** Chromium native patch ACTIVE，而**事实源** `server/fp/nativePatchManifest.js` 实为 **10 个** `enabled: true` / `status: ACTIVE` ⇒ 文档**落后 4 个 patch**（`identity-config-plumbing` / `ua-metadata-platform-identity` / `ua-metadata-mobile-identity` / `cdp-platform-surrender-fallback`）。**漂移成因可考**：README 的「6」定稿于 2026-09-06 的 C8/C9 考古收口（`.benchmark/PHASE16B_ROI_GATE.md` 明写「manifest enabled=6 终版不变」），此后 0008/0009/0010 等 patch 走完 source→patch→build→runtime→行为测试→ownership→回归**全链**翻真，README 未同步。**归因 = B 类（一致性）**：无行为影响、不涉检测站信号（D 类禁止）、非 A 类真实缺陷。修复 = 该行改写为 **10 个全 ACTIVE** 并列出 10 个 surface，同时显式注入「`nativePatchManifest.js` 为**唯一事实源**」+ `enabled` 的**翻真条件**（防再次漂移）。**横向扫描**全部 `*.md`：仅 `README.md` 与 `release/…/README.md`（**gitignored 构建产物**，由 `pack_release.js` 生成 ⇒ 禁手改）命中 ⇒ 改动面 = **1 文件 1 行**。**先证契约安全再改**：三个 README 契约守护 c32 **11/0** / c34 **9/0** / c48 **21/0**（c48 的 P5 `**N/0**` + `OK=N/BAD=0` 模式在 phase9 内再次 PASS）⇒ 改 README **不破坏任何既有断言**。双回归：runRegression **227/0** 全绿（893.3s）+ phase9 **OK=220/BAD=0** 全绿 + treehash **OK=32/BAD=0** 零污染。**断言口径零变化**：未新增 / 未删除 / 未修改任何测试。**未做（如实登记）**：未为「文档数值 × 事实源」建自动化守护（本轮只修事实，守护候选入下批）。
- **C119（超时参数族跨层一致性：执行器超时重基线 + 不变量建立；1×B 跨层矛盾）**：修掉 **C118 修复自身引入**的不一致——C118 把 step22 F1 观测窗 300s→600s，却未同步执行器对该套件的进程超时（C108 设 480s）⇒ **480s < 600s**：若 F1 链长落在 480–600s，断言本会绿却先被执行器杀掉 ⇒ 600s 窗在该区间**永远无法兑现**，错误形态从「假红（读到中间态）」退化为「套件被超时截断」（更难归因）。**★ 这不是理论问题**：F1 链长实测漂移 C108 193.4s → C116 267.9–305.6s → C117 377.6s → C118 345.1s → **C119 354.1s（距旧 480s 仅 126s，余量 26%）**，与 C116 发现「300s 窗余量塌缩」是**同一形态换阈值重演**。修复 = `PER_TEST_TIMEOUT_OVERRIDES` 480000 → **720000**（= 最大内部窗 600s + 120s 余量；≤ 2× 上界防失控；= 实测套件最大耗时 377.6s 的 1.91×），并把**跨层不变量**（执行器超时 > 套件内部最大观测窗）写进源码注释＋保留 C108/C118/C119 三代演进与「✗ 但不变量未同步」失败记录。**断言口径零变化**：只放宽保险丝（超时不参与业务判定），step22 与所有断言行**一字未动**。**横向扫描**（复刻执行器发现逻辑，227 套件）= step22 **唯一**含内部观测窗的套件、F1 `600000` 唯一超全局默认值 ⇒ D1 为孤例但修复必带结构性守护。新增 `test_c119_suite_timeout_consistency.js` **26/0**（A 提取器自洽 5 / **B 硬不变量 4** / **C 横向完整性 4**（扫全部 227 套件，防未来新套件重蹈）/ D 真实行为 3（源码提取 `waitTaskTerminal` 真实函数体实跑）/ E 取证链 4 / F 防空断言 2 / G 隔离 4（含 `maskStrings` 有效性自检））；第一版 **24/1** 如实记录——F 组人造源码字面量直写导致**本文件被自己的测试数据污染**（G3 detail 显示 `[888000,777000]`），改为**运行时拼接**构造并追加 G4 守护。**双向验证五探针全部精确咬住**：超时回退 480000 → B1+B2+C1 / 删 override → A2+B1+B2+B4+C1+C2 / F1 窗提到 900000 → A5+B1+B2+C1 / 新增含大窗套件未登记 → C1+C2（无串扰）/ `maskStrings` 换 no-op → C1+C2+G4（★ 实证横向扫描的前提不是过度设计）。双回归：runRegression **227/0** 全绿（754.4s；step22 **354.1s** 同时满足 `>300s`【C118 修复再次被真实运行验证】与 `<480s`【本次修的矛盾即将被触发】）+ phase9 **OK=220/BAD=0** 全绿 + 前后 treehash **OK=32/BAD=0** 零污染。**★ 方法论**：本缺陷是在写 C118 报告「未修项」一节时暴露的——逼自己写「什么情况下不成立」会暴露未被审视的假设；且**修复可能引入与原缺陷同类的缺陷**（参数随负载漂移而阈值不动），故修复刻意不只改数字。
- **C118（step22 F1 观测窗重基线：兑现父级 MEMORY 登记「未做，须独立批次」的时序余量收口）**：C116/C117 取证证明 F1 恢复链（repair 耗尽 → replan → … → 升级）真实链长随执行环境负载漂移——C108 实测 193.4s → C116 实测 267.9–305.6s（4 红全 >300s / 3 绿全 <300s 完美分离）→ C117 runRegression 实测 **377.6s**，300s 观测窗余量塌到负值：断言在真实终态（`HUMAN_ESCALATION`）到达前截断读到 `RUNNING` = 假红，每次假红都要烧一轮全量复跑证清白。修复 = **仅延长观测窗至实测上界 377.6s + >50% 余量 = 600s**；**断言口径零变化**（`!== 'SUCCESS'` 与 `['FAILED','HUMAN_ESCALATION','PAUSED_FOR_HUMAN']` 终态白名单一字不动——只延长观测、不放松裁决）；`waitTaskTerminal` 终态 1s 轮询早退 ⇒ 绿路径零成本，只有真悬挂才耗满窗。**守护测试 `test_c118_step22_window_margin`（13 断言，tmp 隔离·零浏览器·零 LLM·零网络·零业务模块 require）**：从源码提取 `waitTaskTerminal` 真实函数体 `new Function` 注入 stub api 实跑——A 终态早退/超时回传原始 RUNNING 不伪造终态/`extraTerminals` 合并 3；B 观测窗锚点 4（F1=600000 落盘、旧 300000 清零、其余等待窗保持 180000 恰 4 处无连带放大、源码无残留半截口径）；C 裁决口径零变化 3（`!== SUCCESS` + 终态白名单 + `reverified success===false` 断言全部在位）；D 余量算术自检 3（窗 ≥ 实测上界 1.5× 且 ≤ 2× 防失控放大、取证链注释 377.6/267.9/193.4 保留防窗口变成无源之数）。**双回归**：runRegression **226/0 全绿（748.5s）**——step22 `PASS=63 FAIL=0` 且 **F1 实测 345.1s 到终态，恰好落在旧 300s 窗之外（正是此前必假红的形态），新窗直接验证有效**；phase9 **OK=219/BAD=0 全绿**，双历史最佳。
- **C117（无界集合治理：queue 终态 TTL + session TTL；1×B 治理原语缺失 + 1×B 归档可访问性）**：兑现 C115 登记的两个 `UNBOUNDED_ACCEPTED` 边界（`aiQueue`/`aiSessions`）。**★ 为什么它们长期只能「如实登记」而不能治理**——不是忘了配水位，而是**既有归档原语的语义不适用**：`archiveOldest` 按「数组头部 + count」切分（`data.slice(0, count)`），而队列是「**主文件即工作集**」（`dequeue()` 只读主文件）⇒ 位置切法会把仍在 `PENDING` 的任务移出主文件 = **任务静默不执行**（比无界增长更糟）。**① D3（B/缺原语）** 新增按**谓词**切分的存储原语 `archiveWhere(name, pred)`（`store.interface.js` no-op 默认 + `jsonStore.js` 真实现，复用 `_archiveAppend` 保住损坏侧车保全 + 原子写；单遍分拣保证 `moving + remaining === 原长度`；**谓词抛异常时在任何写入之前中断** ⇒ 主文件与归档零变化，fail-safe 不留半截状态）。两原语作用域由治理表结构互斥（`AUTO_ARCHIVE_LIMITS` 与 `BOUNDED_COLLECTIONS` 无交集），不存在「既被 `write()` 自动归档又被 prune 归档」的双写路径。**② D1（A/无界增长）** `queue.js` 新增 `pruneTerminal`：**只归档已终结（`DONE`/`FAILED`/`CANCELLED`）且「超龄 >7d ∨ 超出终态封顶 500 条」的记录，`PENDING`/`RUNNING` 永不归档**（终态白名单是承重不变量）；`createdAt` 缺失/非法的终态记录**不动**（判断不了的不该被静默扫走）；在 `enqueue` 的**唯一自然写点**顺带触发（单一调用点，避免两处 `return` 各自漂移）；**归档失败绝不阻断入队**但必须 loud（`console.warn`）。**③ D2（A/无界增长）** `sessionManager.js` 新增 `pruneSessions`（`updatedAt` 超龄 >30d ∨ 超出封顶 200 条），`createSession` 顺带触发。**④ D4（B/归档不得引入行为破坏）** 会话是**用户可回访**的对象：归档若让 `getSession` 返回 `null`，`index.js:511`（`sessionId ? getSession(sessionId) : null`）会**静默新建会话**——用户点开旧对话看到空白、全程无报错 ⇒ 配套 `findInArchive` + **`getSession` 在主集合 miss 时从归档原位恢复**（归档 = 纯磁盘卫生，不改变可访问性；恢复后 `addMessage`/`attachTask` 走既有路径）。**⑤ 治理登记迁移**：两项由 `UNBOUNDED_ACCEPTED` 迁入 `BOUNDED_COLLECTIONS`（三表并集仍 == `FILES` 34 = AUTO 14 + BOUNDED 20 + UNBOUNDED 0），`UNBOUNDED_ACCEPTED` 表**保留但清空**（不删表、不塞进别处）。**守护测试 `test_c117_queue_session_ttl`（65 断言 / A–J 组，tmp 隔离·零浏览器·零 LLM·零网络）**：A 原语语义 10（无命中零写 / 谓词异常零变化 / 损坏归档跳过且只读 / 全坏不抛）/ B 终态白名单 5 / C 时间+数量双条件 6（含**真重叠**构造：`|expired|=3 ∪ |overflow|=4 ⇒ 4 而非 7`，朴素相加即红）/ D 归档不丢数据 5（总数守恒）/ E 故障注入 4 / F `dequeue`·`enqueue` 语义零破坏 6 / G 静态锚 7（含**防空断言自检**：剥离后注释里的 `C117` 已消失而真代码仍在）/ H 治理登记 6 / I 隔离零污染 3 / J 会话行为 10（原位恢复、未知 id 不伪造、失败 loud）。**双向验证五探针（全部精确咬住、互不串扰）**：终态白名单失效 → **B1+B2/B5/B6/G2 红**（即「PENDING 被归档」这一核心危害本身）；双条件不做并集去重 → **C4/D1/B3/B5/F4 红**；会话归档兜底失效 → **J4b/J5/G5 红**；治理双重登记 → **H3/H5/H6 红**；谓词异常被吞 → **A5 单条红**。**双回归**：受影响既有套件 **25/25 OK**；`runRegression` **224/1**（共 225 项，唯一红 = 既有 `test_step22` F1 **时序余量塌窗**：377.6s > 300s 观测窗 → 断言读到 `RUNNING`；**独立复跑 PASS=63 FAIL=0 全绿且同一条断言得到 `HUMAN_ESCALATION`** —— 同断言同代码、不同环境不同结果 ⇒ 归因环境时序 flake，按纪律**未改任何 timeout/断言**；两种情形下「未误判 SUCCESS」的安全属性均成立，`.benchmark/c117_step22_rerun1.log`）；`phase9` **OK=218/BAD=0 全绿**；前后 treehash `OK=30 BAD=0` 零污染（`.benchmark/c117_runregression.log` / `c117_phase9.txt`）。
- **C108（mock plan strict 契约修复）**：收口 C79 登记的真实 A 类缺陷——`provider.mock planForTask` 返回「规范化运行时 Step」（action 为对象、顶层 `type: 'NAVIGATE'`），而 `planner.planObjective` 按「严格 Step 契约」消费（`normalizeStrictToCanonical`：顶层 action=动作字符串 + semantic/expectedResult）→ 归一后全部步骤退化为 ACT/空 action/空描述 → `validatePlan` 恒拒绝 → **mock planObjective 恒失败**：/chat mock 模式恒 400（session 创建后规划阶段失败）、runtime REPLAN 恒 fail-fast（恢复链「意外地快」是死路径副作用，不是性能）。修复：planForTask 产出严格 Step 契约（与真实 LLM provider `deepseekPlan` 同走 `validatePlanStrict` 自校验 fail-loud，契约漂移在生成期炸掉而非校验层静默重试三次后 400）+ 凭据契约对齐（有 ref → email/password 身份字段一律 credentialRef 禁 value 编造；无 ref → 零 credentialRef 且敏感字段动作不规划，空集反向守卫）。**附带时序重基线（C79 专门批次承诺的收口）**：mock REPLAN 变为真实可用 → step22 受控失败注入场景（F1）恢复链多走「repair 耗尽 → replan → 执行 → 再失败 → 升级」全程，实测 **193.4s 到 HUMAN_ESCALATION**（c108_run1/run5/run6 三跑一致，事件级证据 `.benchmark/c108_step22_run*.log`）；`runRegression` 对该套件启用**专属超时覆盖**（480s，仅执行时间窗对齐真实恢复链成本，**断言口径零变化，非降阈值**）+ step22 等待窗 180s→300s 观测真实终态（`PAUSED_FOR_HUMAN` 计入显式交人终态）。守护测试：`test_c108_mock_planner_contract` **21/0**（P1 空集反向守卫 + P2 planObjective 全链 NAVIGATE 打头/入口地址保真/描述非空 + P3 凭据引用对齐 + P4 引用不可用清单不误伤 + P5 漂移守卫 + P6 REPLAN 死路径激活实证 + P7 真实服务器 /chat 200+taskId+计划挂载，tmp 隔离零浏览器）；既有断言升级到修复后行为（修断言不放宽阈值）：`test_c79`（chat 400→创建 session+任务归属取证）、`test_c84`（P1d 400+审计留存 → 200+taskId 且审计锚定任务 id，更强归属断言）。双回归全绿 **214/0（699.7s）+ OK=207/BAD=0** 双历史最佳（`.benchmark/c108_runregression3.log` / `c108_phase9.log`）。
- **PHASE 17-A（决策消费 + 凭据安全）**：两个 P0 落地并双回归全绿 **213/0 + OK=206/BAD=0**。**P0-A Credential Action Authorization Gate**（新模块 `server/agent/credentialAuthorization.js`）——R6 实证「误点第三方授权入口 → 导航到 `github.com/login` → 继续把环境凭据写进第三方域输入框 4 次」；废弃 C106 F21「双向子域包含」判据（字符串相似 ≠ 授权关系），改建 `Task → Authorized Flow → Allowed Origin → Credential Action` 显式授权上下文：十级判定（非凭据动作放行 → 授权上下文缺失 / originless 本地上下文 / origin 不可解析 / 挑战页 / 跨 origin iframe → 显式授权 → 第三方 OAuth 面不继承主站授权 → 运行期授予 → opt-in 流程跳转 → 其余拒绝），无法证明 AUTHORIZED 一律 **fail closed**；闸门在 `tools.js` 三处接线（动作前 + 定位后 + 目标元素所在文档 origin 复检），runtime 对 `CREDENTIAL_ACTION_BLOCKED` **不重试/不 repair/不 reload**，直接 `escalate` 并写安全 evidence（不落凭据值）。**P0-B Diagnosis Decision Gate**（新模块 `server/agent/diagnosisDecision.js`）——R3 实证「LLM 已正确诊断『分步表单，密码框尚未出现』(conf 0.95)，Runtime 仍机械重放 `fill password` 12 次 / 473s」；六态 `STATES`（TARGET_NOT_PRESENT_YET / MULTI_STEP_FORM / NAVIGATION_IN_PROGRESS / CROSS_ORIGIN_DRIFT / SECURITY_CHALLENGE / TARGET_STALE）→ `POLICY`（block/require/allowAdvance/maxRepeats/escalate/noRepair/credentialOnly），诊断**拥有 BLOCK CURRENT ACTION 的能力**；刻意**不是 Verification**——`evaluate` 输出无 `success`/`verified` 字段，只能改下一步动作策略，不能产出 SUCCESS、不能改 Success Definition、不能绕 Verification。**Anti-flapping 最小修复**：同失败签名 + 决策未变 + `noRepair:true` → 直接 escalate，**只停止重复 repair，不重写 repair engine**（历史同签名可空转 21 次 repair）。守护测试：`test_c107_credential_boundary` 34/0（契约 + 静态断言：无站点名字面量 / 无残留子域包含判据 / 闸在观察之前）+ `test_c107_credential_boundary_fixture` 17/0（真实 Chromium 闸门层）+ `test_c107_r6_crossorigin_replay` 21/0（真实 runtime 端到端：第三方域 password fill **4→0**、页面余额 **空**、repair **反复→0**、reload **无限→0**、耗时 **长循环→3.4s**、终态 `HUMAN_ESCALATION`）+ `test_c107_diagnosis_consumption` 18/0（R3：password fill **12→2**、repair **21→0**、**473s→20.6s**）+ `test_c107_anti_flapping` 17/0（含**负向对照** `repairCalls` 正例 0 vs 反例 1，排除「repair 链路本身坏掉」的假绿）。**附带修复一类系统性测试缺陷**：多个既有 fixture 用「不存在的硬编码/动态 taskId」调 `tools.runTool` 填凭据字段 → P0-A 上线后 fail closed（**正确行为**），修 fixture 而非放宽阈值（`test_c71_tools_hardening` 20/3→**23/0**、`test_step6_payment_capability` 139/5→**144/0**（表单改挂真实 HTTP origin，顺带把端到端 fill 变成真正穿过 Gate 的实证）、`test_c106_f16_f17_fixture` 9/4→13/0、`test_c106_f20_typing_fixture` 4/9→13/0）；`test_step22_business_e2e` 的两轮独立复跑证明其 4 项失败为**启动时序 flake**（63/0 ×2），`test_gen_pool_v2` 为 `core.autocrlf=true` 的**一次性检出行尾产物**（24/0）。**未做**：真实 Webflow E2E（PX 挑战页恒返回，属环境限制，不允许绕过）——等人工授权。
- **PHASE 17-C（Project Skill 基础骨架，不接执行）**：按 17-B 报告 §25.1 授权的第 1–4 项落地「**先落数据，后落执行**」——**只产 `CANDIDATE`，无 SkillRouter / 无 SkillExecutor**。新增 `server/agent/skill/`（1255 行 / 5 模块）：`skillSchema.js`（SEC1–SEC8 安全闸 + 结构校验，纯函数零 I/O 零 LLM）、`skillEvidence.js`（五要素证据 + 独立性判定，独立集合 `aiSkillEvidence`）、`skillLifecycle.js`（PROMOTION/STALE 阈值 + `skillConfidence`，**不做状态转移**——转移属 17-D）、`skillBuilder.js`（**确定性提炼，无 LLM**）、`index.js`（**刻意不含 Router/Executor**）。新集合 `aiSkill`（主记录，规模 10²–10³ **刻意不设水位**）/ `aiSkillHistory` 2000 / `aiSkillEvidence` 3000 / `aiSkillRuns` 4000（防重演 aiAttempts 42MB 事故）。消费点 = `taskManager.complete()` 内与 `flowMemory` **并列** fail-open 调用 `skillBuilder.observe(task)`（只消费 complete 路径 → 假成功不入 Skill；Skill 无独立执行链 ⇒ **结构上不可能绕过 17-A 凭据闸**）。**G2（单次成功即晋升）的解法是改公式而非降阈值**：阈值 0.85 原样保留，未过门禁时部分分 = `ind/2*0.4 + sess/2*0.2` 且**封顶 0.84** → 1 成功/1 会话 = 0.300、**2/2 = 1.000（唯一跨阈值点）**；独立性三要件 = 不同 `executionId` ∧ 不同会话 ∧ 时间差 >10min（按 executionId **去重**）。两处与设计稿的**刻意偏差**已在代码注释记录：[D1] SEC1 只扫可执行体（不含 boundaries，否则 §16.1 强制的 `requiresHumanOn: MFA/3DS` 会误拒任何合法 Skill）、[D2] SEC6 只拒**裁决型**断言（不拒 `samples.success` **计数**字段，否则统计基座无法落库）。**顺带修复一处真实死护栏（同源两处）**：`FORBIDDEN_VALUE_HINTS` 含 `'document.querySelector'`（大写 S）而比较前 blob 已 `toLowerCase()` → **恒不命中**；取证 `aiFlowMemory.json` 对三种禁用形态零命中 → 纯收紧、行为中性；`skillSchema.SEC7` 与 `intelligence/flowSchema.js` 同因同修 + 守护断言。守护测试 `test_c109_skill_schema` **62/0** + `test_c109_skill_builder` **75/0**（137 断言，含正向对照与「证明真正执行到写路径」的 GATE 断言）。**顺带修复回归执行器的环境非确定性缺陷（零断言改动）**：后台/嵌套 shell 下 `TEMP`/`TMP` 可能缺失或为 POSIX 形态 → `os.tmpdir()` 回落「可创建但不可列」的 `%SystemRoot%\temp` → esbuild 解析 `stdin.resolveDir` 父目录 Access denied → 8 个 SSR/esbuild 套件（c70/c74/c78/c80/c81/c82/c87/c88）整套假红；新增唯一事实源 `server/scripts/tmpEnvGuard.js`（`%LOCALAPPDATA%` → `os.homedir()` → 仓库内 `.benchmark/.tmp` 兜底，win32 显式拒绝 POSIX 形态），`runRegression.js` 与 `run_phase9_regression.sh` 共用，env 正常时零行为变化。双回归 **217/0 + OK=210/BAD=0**。**未做（待人工授权）**：SkillRouter（17-D）/ SkillExecutor（17-E）/ 真实 Webflow E2E / canonical240 重基线 / 模型升级 / benchmark 修改。
- **PHASE 17-D（Router 五级判定 + 三态预检，影子模式，**不接执行**）**：按 17-B 报告 §25.1 第 5–7 项落地。新增 `server/agent/skill/skillRouter.js`（656 行，**纯函数 + fail-open 落库**）。① **五级判定**：Intent 归一化 → Capability 匹配 → Environment 适用性（不匹配 = 不选它，**不是失败**）→ 置信度/陈旧过滤 → State Contract 预检（★G1）；② **三态预检**（C105 教训的强制落地）：`MATCH` / `MISMATCH` / **`INDETERMINATE`**——无观察 / 空 URL / `about:blank` / `data:blob:` / 非 http(s) / 无元素池 / `loadingState='loading'` 六类触发条件一律走 Generic **且不记录任何 Skill 失败**（「空集 ≠ 不存在」：把第三态当 MISMATCH 会重演 C105 F2「延时挂载按钮 → 30s 超时 → reload → BLANK 页死亡螺旋」）；**FALSE 优先于 INDETERMINATE**（AND 语义下确定性否定足以定论，否则真不匹配会被误判成「不确定」从而绕过 STALE 判定）；③ **五条决胜规则** `ENV_SPECIFICITY → CONFIDENCE_DESC → LAST_SUCCESS_DESC → REQUIRED_CLAUSES_DESC → **REFUSE**`（显式拒绝平局——C105 D-A「同分按 DOM 顺序决胜 → 永远点第一个 button」是缺陷温床）；④ **独立性判定**（`aiSkillRuns` 为唯一数据源）。新集合 `aiSkillRouting`（水位 4000；每任务至多一条含回填）。**影子闭环 = 决策期写入 + 终态回填**：`runtime.resolvePlan` 内**复用已有 `planningObs`**（零新增导航 / 零新增观察成本 / 零新增失败模式）调 `router.shadow()`；`taskManager.complete/fail/escalate` 调 `recordRoutingActual()` 回填 `actual`（SUCCESS / FAILED / HUMAN_ESCALATION）——只有两侧都在才能比对「Router 会怎么决定 vs 实际结果」（§25.1 门禁）。**三层结构保证决策不改变执行路径**：模块导出面无执行 API + 调用点返回值未赋给任何变量（`try{…}catch{}` 内）+ 静态断言 `stripComments` 后零执行旁路；**端到端路径证明**（§25.3「必须证明真正执行到了目标代码路径」）用真实 `resolvePlan` / `complete` 调用链实测写入与回填（`.benchmark/phase17d_e2e_probe.js` + `phase17d_e2e_path_proof.txt`）。**实现期四修**：⚠️ **预检目标修正（不修则预检形同虚设）**——预检对象是「**第一个可执行状态**」而非「入口状态」（builder 产出 `entryState='LANDING'` 且其契约**全 SOFT**（A/B 测试与多入口情形刻意不参与状态拒绝），直接对入口预检会让**任何** Skill 都得到 `NO_REQUIRED_CLAUSE → INDETERMINATE`）；`wouldPromoteReasons` 未随 `route()` 输出映射（影子数据缺诊断）；**决策字段与观测字段必须分离**（平局拒绝时 `skillId` 曾仍指向影子候选 → 拆 `skillId`（决策，仅 `decision=SKILL` 时非空）/ `observedSkillId`（观测），**避免「拒绝」被误读成「选了某一个」**）；`OBSERVABLE_TYPES` 补 `url_contains`（17-C 遗留：该类型是 `deriveContract('navigate')` 的权威契约，缺失导致导航步被整步丢弃）。**并发协作处置（如实记录）**：工作区出现**非本会话**改动（`skillBuilder.js` mtime 晚于我方最后编辑 + 提交 `a3f4398` C111 证据链引用悬挂修复）——处置 = ①取证缺陷真实性（当前 16 skill / 16 chain、悬挂 0 条 → 缺陷真实但需达水位 3000 才触发）②验证修复行为中性（旧链存在时重指后 ref 值不变）③补 **CH0–CH4 五条守护断言**使其不再是「无守护的孤儿改动」（builder 套件 75 → **80/0**）④报告中标注来源。守护测试 `test_c110_skill_router` **85/0**（P0 4 / T8 三态 15 / X 预检目标 3 / G1 逐级 12 / K 决胜 12 / E 影子 17 / T9 不污染统计 6 / G2 独立性 5 / S 静态 13）。**双回归**：`phase9` **OK=212/BAD=0**；`runRegression` 轮 1（上会话遗留编排链）**218/1**，唯一失败为 `test_step22_business_e2e` 的 **F1 恢复链时序 flake**（`PASS=62 FAIL=1`，断言 `F1 任务进入显式交人终态（RUNNING）`，379.9s 仍未收敛）→ 按既有纪律**独立复跑证清白**（**63/0**，`RUN_ID=run_mtw869bj`，`F1 → HUMAN_ESCALATION` ✓，`.benchmark/phase17d_step22_rerun.txt`）后**全量复跑取干净基线 219/0**（853.0s，零失败零 flake）。**★ 结构性事实（必须固化）**：`builder` 恒产 `CANDIDATE` 且 17-D **不做状态转移**（转移属 17-E）⇒ ④ 级门禁过滤掉所有 Skill ⇒ 当前 `decision` **结构性恒为 `GENERIC`**；但 `prestateScope='SHADOW'` 仍产出有效预检信号，17-E 一接晋升即可立刻拿到「**晋升前 N 次任务本可以用 Skill**」的历史证据而不必空跑。**未做（待人工授权）**：SkillExecutor（17-E）/ POC-2 与 Generic vs Skill A/B（17-F）/ 真实 Webflow E2E / canonical240 重基线 / 模型升级 / benchmark 修改。
- **PHASE 17-E（SkillExecutor / Handover / Lifecycle，受监督接管）**：按 17-B 报告 §25.1 第 8–10 项落地。新增 `server/agent/skill/skillExecutor.js`（1023 行）。**① 七步契约**：`1 ROUTE → 2 PRECHECK → 3 EXECUTE → 4 OBSERVE → 5 VERIFY_STATE → 6 HANDOVER/CONTINUE → 7 FINAL_VERIFICATION`；ROUTE 复用 17-D `router.route(...)`（**不重实现 Router**），VERIFY_STATE 复用 `router.contractVerdict`（**不新建验证器**）。**② §3 结构限制的实现取舍（本阶段最重要的工程判断）**：全项目唯一全管线入口是 `tools.execute`（validateAction → policy.allowsAction → Resource Lock 持有校验 → contextGuard → runTool → 证据快照 → elementMemory 挂起确认），**裸 `runTool` 是它的内部环节，直调会绕过 policy 与资源锁**——那才是真正的旁路。因此采取「**监督者**」形态：SkillExecutor **不持有任何执行入口**（require 面只有 `store` / `skillRouter` / `skillBuilder` / `skillLifecycle` / `skillEvidence`；无 browserManager / 无 page / 无 tools / 无 credentialAuthorization / 无 selector engine），唯一产出 = 「这一步用 Skill 的语义动作（`actionOverride`）还是 Generic 的动作」+「何时停止接管」；**物理执行仍由 Runtime 走既有 `runStep → tools.execute`**，policy / 凭据闸 / 观察 / 验证全部原样保留。结构上「Skill 绕过 17-A 凭据闸 / 绕过 `verification.js`」**不可能发生**（守护组 N 静态断言）。**③ 准入负向清单**（§4.2）：只有 `decision===SKILL ∧ prestate===MATCH` 才进入；`MISMATCH → PRECHECK_MISMATCH`；`INDETERMINATE → PRECHECK_INDETERMINATE` 且**不记任何 Skill 失败**（「观察不可用」的判定**优先于** prestate 取值——观察不成立时 prestate 本身不构成确定的不匹配）；`NOT_ACTIVE` / `LOW_CONFIDENCE` / `STALE` / `REFUSE` / `NO_ELIGIBLE_CANDIDATE` 一律 HOLD。**④ 每步重新 Observe 零成本**：`runStep` 返回值里的 `observation` 就是 `tools.execute` 的 `_enrich(...,'after_action')`（`fresh=true`）⇒ `afterStep` 直接消费（C105 stale selector / reload 死循环的核心修复纪律）。**⑤ §7 成功裁决权 100% 留在既有 `verification.js`**：导出面无 `execute`/`run`/`declareSuccess`/`markSuccess`/`setSuccess`；步级产出只有 `STEP_COMPLETED` / `STEP_HANDOVER` / `STEP_BLOCKED` / `STEP_FAILED`；按钮点击成功 / 页面变化 / 表单提交 / URL 改变**都不是** Business Success。**⑥ Handover 契约**：25 条结构化原因（§9 指定的 11 条逐条在册，无 `something went wrong` 类占位）；工具层错误码未命中映射时回落 `SKILL_STEP_FAILED` **并把原始 code 写进 `causeCode`**（绝不吞掉真实失败码）。**⑦ 防循环 `SKILL_HANDOVER_ONCE`**：判定依据是**已持久化的执行记录**（不是内存标志）⇒ 跨进程重启成立且可独立审计（`handoverCount === 1`）；Runtime 侧交还后即置空 session。**⑧ 生命周期状态机**（`skillLifecycle` 只做纯判定，持久化在 executor）：`CANDIDATE → ACTIVE → REVALIDATING → (ACTIVE | STALE) → DEPRECATED`，合法迁移**白名单**、表外迁移一律不产生。**⑨ 阈值 0.85 与公式零改动**：晋升仍走 17-C `promotionGate`（≥2 次独立成功 + ≥2 独立会话 + 时间差 >10min + 候选期零失败 + 证据完整 + 契约观察 ≥2），`skillConfidence` 未过门禁仍封顶 0.84。**⑩ 非磨损纪律**（§11/§15/§17-B）：`INDETERMINATE` / `AUTHORIZATION_BLOCKED` / `NOT_ATTRIBUTABLE` 三类结果**不迁移状态、不计失败、不磨损 confidence**——且对非磨损结果**连 confidence 都不重算**（重算是拿当前 runs 重跑公式，集合一变数值就变 ⇒ 等价于「一次授权阻断改变了 Skill 置信度」，正是要杜绝的激励）；`AUTHORIZATION_BLOCK_AFFECTS_STATUS === false` 从根上切断「为让 Skill 能用而弱化安全闸」的优化压力。**⑪ 归因纪律**：**延迟确认**（17-B 纪律上移到 Skill 级）——Skill 动作成功什么都不记，只有任务真正到达**既有口径**终态才结算；且** handover 过的执行即使任务成功也不给成功分**（「Skill 交还 Generic、Generic 成功」不是 Skill 的功劳，否则会激励 Skill 抢活）。**⑫ 两处刻意的错误码分类**：`ELEMENT_NOT_FOUND → INDETERMINATE`（**不磨损**，C105 #3「空集 ≠ 不存在」：快照分不清「尚未挂载」与「永久不可用」）vs `TARGET_AMBIGUOUS → VERIFIED_FAILURE`（**磨损**：歧义是**确定性**负面信号「目标不够精确」，且受 §15 保护——ACTIVE 单次失败不迁移）。统一原则：**只有「确定性否定」才磨损 Skill；「无法判定」一律不磨损。** 新集合 `aiSkillExecutions`（水位 2000：记录内嵌逐步证据，体积大于路由记录，防重演 `aiAttempts` 42MB 事故）；证据链本体落既有 `aiSkillEvidence`，execution 只持 `chainId` 外键（摘要 + 引用，**不存全文 / 不存定位符 / 不存值**）。**实现期两修**：⚠️ **零新增导航成本**——初版把 0 级预闸放在 `resolvePlan` **之前**并自行 `capturePlanningObservation`，会给 Skill 路径引入一次 **flowMemory 本可避免的导航**（`resolvePlan` 的 flow 命中分支在采集观察之前 return）→ 新增 `takePlanningObs(taskId)` **复用**同一份观察（flow 命中时拿到 null ⇒ Router 判不可用 ⇒ `HOLD + PRECHECK_INDETERMINATE`，语义正确且诚实）；▲ 测试夹具泄漏——K6 的漂移结算**正确地**把 confidence 降到 0.667（`failed=1` ⇒ `2/3`），但组间复位函数只复位 status/计数器没复位 confidence ⇒ 后组撞上 0.85 阈值（**夹具缺陷，反向证明阈值与公式真的在生效**）。守护测试 `test_c113_skill_executor` **112/0**（A Executor contract 9 / B MATCH 9 / C MISMATCH handover 4 / D INDETERMINATE handover 5 / E State drift 6 / F Handover once 4 / G Lifecycle 7 / H Promotion independence 11 / I STALE·REVALIDATING 8 / J Credential Authorization 9 / K Verification authority 9 / L No-selector persistence 6 / M Evidence chain 7 / N No execution bypass 13 / FX 夹具自洽 4 / T24 隔离零污染 1）；并发 C111/C112 三条覆盖 Skill 域（builder 80/0 · schema 62/0 · router 85/0 · evidence 21/0 · pairing 20/0）全绿。**双回归**：`phase9` **OK=214/BAD=0**；`runRegression` **221/0**（719.4s，零失败零 flake）。**⚠️ 17-E 无关的两个 flake（归因链完整记录，防重复排查）**：① `test_fp16b_c2_unit` 单轮 `FATAL browserType.launch: Target page, context or browser has been closed`（Chromium 启动瞬时失败）→ **独立复跑 3 次全绿（pass=17 fail=0）**，环境瞬时 flake，零改动；② `test_step22_business_e2e` 单轮 `FAIL: aiAttempts 中存在 e3 失败记录（0 条）`——**决定性反证**：该轮 `aiSkillExecutions.json` **根本未创建**（⇒ SkillExecutor 全程未接管，17-E 接线在该轮可证伪地惰性），且 `aiAttempts.json` 中**确实存在 4 条** `_e3` + `TOOL_EXECUTION` 记录；**根因 = 测试自身的 id 子串碰撞**（用 `String(e.stepId).indexOf('e3') >= 0` 过滤，而 Scenario E 的 **taskId 本身含子串 `e3`**：失败轮 `task_mtxq**e3**upk3bp` → 匹配到该任务**全部**步骤 → `started3[0].stepId` 解析成 `..._e1`（err=null）→ 回查 0 条；对照 4 轮通过轮 taskId 均不含 `e3`）。按 §32 纪律**本阶段不改该断言**（明令「出现 flake 不得修改 timeout / assertion」，且该文件不属 17-E 面）→ 独立复跑 **63/0**（`run_mtxr5r96`）+ 全量复跑取干净基线。**★ 结构性事实**：`builder` 恒产 `CANDIDATE` ⇒ `eligible().any` 恒假 ⇒ **生产路径上 `decision === SKILL` 不会发生**（设计上的正常状态，17-E 正确性由显式 ACTIVE 夹具驱动）；**未做（待人工授权）**：PHASE 17-F / POC-2 / Generic vs Skill A/B / 真实 Webflow E2E / canonical240 重基线 / 模型升级 / benchmark 修改。
- **C116（跨切面水平复审：备份双根覆盖 + 数据根单一事实源；1×A 真实数据丢失 + 1×B 一致性 + 1 边界登记）**：换轴复审——C108–C115 走纵深（工具粒度→参数级→存储 I/O），本批次挑一个**横切关注点**「数据根解析」在**全部消费模块**上做一致性核查。**① D1（A/高危·真实数据永久丢失）** `backup.js` 只解析 **db 根** `<repo>/data`，而 AI 集合（`JsonStore` 的 `FILES`：`aiExecutions`/`aiAttempts`/`aiTasks`/`aiSteps`/`aiPlannerEvidence`/`aiSkill*` …）按设计住在 **AI 根** `<repo>/server/data`（`.gitignore:22-23` 明文）⇒ 实测 AI 根 **32 个顶层集合 / 8,787,208 B** 真实数据 vs db 根同名文件 **22 个是 2 字节空桩 `[]`**、另 **5 个集合在 db 根完全不存在**；导出得到空集合、恢复把空集合写回、**全程报成功**——换机/重装后 agent 全量记忆、Skill 库、执行记录、证据链静默蒸发而备份文件「看起来完好」。原注释「含 `ai*` 全家」是**假陈述**。修复 = `collectSnapshot` 覆盖两个根 + 每文件记 `fileRoots`（v2 自描述：恢复端不依赖路径也不依赖当前注册表）；`restoreSnapshot` 按根写回原位 + **逐根**防呆 `pre-restore-<ts>`（此前只快照 db 根）＋ `opts.roots` 测试注入面；`BACKUP_VERSION` 1→2 且**同时接受 v1**。**★ v1 兼容必须语义不变**：v1 里的 `ai*.json` 是空桩，若「按集合名路由回 AI 根」（看似更正确）会用 `[]` 覆盖 1.7MB 真数据——**从「备份缺数据」升级为「恢复毁数据」**，故 v1 **一律回 db 根**（宁可旧备份继续少数据，也绝不毁数据）。**② D2（B/一致性）** AI 根此前**没有具名事实源**，同一语义被 4 处各自复制（`storage/index.resolveDataDir` / `stepManager` / `archiveAiStore` / `migrationJsonToSqlite`）——正是 D1 的土壤 → 新增 `dataRoot.js:aiStoreRoot()` 与既有 `dataRoot()` 并列，**8 个消费模块**统一 require 消费、零内联（新增导出 `storage/index.dataDir`、`stepManager.evidenceErrorDir` 供守护断言）。**③ 边界 D3（不修、已量化）** 快照范围恒为「数据根顶层 `*.json`」，子目录一律不进，实测约 **5.9 GB 永不进备份**（db `profiles/` 76,185 文件 4.75 GiB、db `evidence/` 12,251 文件 753 MB、AI `archive/` 76 MB …）⇒ **本备份不构成完整灾备**，恢复成功 ≠ 回到故障前全量状态（已在源码注释定性与定量登记）。**★ 为什么 D1 能长期隐身**：所有测试都在 `FPB_DATA_DIR` 隔离下跑，而隔离使 `dataRoot() === aiStoreRoot()`——两个根**塌缩成一个**，双根语义**从结构上不可观测**；**隔离测试的绿灯在此是伪证**，正解不是更多测试而是提供一个能表达该差异的**注入面**（`opts.roots`）。**守护测试 `test_c116_backup_dual_root`（38 断言 / 8 组，tmp 隔离·零浏览器·零 LLM·零网络）**：A `aiStoreRoot` 语义 5（含子进程删 env 实测默认值、隔离下 `legacyDataRoot` 仍指真实 `server/data`）/ B 注册表不相交 4 / C 快照取真数据 8（修复前必红）/ D 恢复写回原位 + **逐根**防呆 6 / E **v1 不得毁数据** 3 / F 拒绝路径不落盘 2 / G 静态锚 7（8 模块零内联）/ H 隔离零污染 3。**双向验证三探针**：`collectSnapshot` 收窄为只扫 db 根 → **C1/C2/C5/C6/C8 精确红**；v1 按注册表路由 → **E1/E2/E3/G6 红**（E2 失败详情 `[]` 即危害实测证据）；`identityStore` 复辟内联 → **P5b/P5c 红而 P5a 保持绿**（定位精准）。**断言过时 ≠ 缺陷**：D2 使 `test_c46` 的 P5 三条**文本锚**失效（`count=0`）而行为断言 P1–P3 全绿证明语义未变 → **不删断言、不降标准**，锚点上移到单一事实源并**加强**（旧「字面量出现过」→ 新「认知集中化 + 零内联 + 派生恰 1 处」）＋新增防空断言 P5d ＋ `stripComments`（与 `test_c116` 同源同语义），`6/3 → 10/0`。**回归**：`runRegression` **224/0 全绿**（731.5s；C115 = 223/0，+1 = 新套件）；受影响既有套件 **9/9 OK，172 断言 / 0 失败**；phase9 **`OK=216/BAD=1`**——唯一 BAD 为**既有** `test_step22_business_e2e` F1，已用**落库真实耗时**完成确定性归因（7 次观测完美分离：4 次红全部 >300s 而 3 次绿全部 <300s，终态**每次都**是 `HUMAN_ESCALATION`；207s→305.6s 的链长把 C108 设定的 300s 窗口余量压到 ≈0，且 phase9 harness 下因前序 216 套件残留资源而 3/3 稳定越窗），**按纪律未修改任何 timeout / 断言**。前后 treehash 零污染。报告 `.benchmark/C116_BACKUP_DUAL_ROOT_REPORT.md`。
- **C115（存储 I/O 数据完整性硬化：2×A 类数据丢失 + 3×B 类治理/隔离）**：跨切面水平复审产出，专找两类已知 A 类模式的**残留点**。**① D1（A/数据永久丢失）** `jsonStore.read()` 的 `JSON.parse` 失败分支此前直接吞 `fallback`，坏的主集合文件**无侧车保全** → 紧随的 RMW（`insert`/`upsert`/`update`/`remove`/`appendEvent`）把「空集合 + 新记录」覆写回去，损坏前全量数据永久蒸发且无从取证；同文件 `_archiveAppend` 早已有侧车（C60 D2 修的），主集合反而没有——C60 只堵住「瞬时锁导致的读失败」，没堵「文件内容真损坏」这个同后果面的入口。修复 = 新增 `preserveCorruptSidecar()`，先 rename 成 `<file>.corrupt-<ts>`（同名追加序号、绝不覆盖）再返回 fallback，**返回值契约一字不改**（`test_c60` P3「真损坏→fallback」仍绿）。顺带兑现 `fsSafe.js:9-10` 预留的「后续如做统一，需单独批次回归 jsonStore 全套件」：删掉本文件自持的 4 个 I/O 原语副本（与 `fsSafe` 逐字对齐），改 `require('../../fsSafe')`，零语义变更、导出面不变。**② D2（A/无界增长）** `aiIntelligenceEvaluations`（Runtime 每任务终态 `collect()` 一条）与 `aiDispatchExecutions`（每次派遣一条）注册了 `FILES` 却从未给水位 → 补 4000。**③ D5（B/治理缺失）** `FILES` 与 `AUTO_ARCHIVE_LIMITS` 之间零一致性约束正是 D2 的成因 → 新增三表登记（`AUTO_ARCHIVE_LIMITS` 14 + `BOUNDED_COLLECTIONS` 18 + `UNBOUNDED_ACCEPTED` 2 = `FILES` 的 34 键）与 `storageGovernanceGaps()` 校验：新增集合三张表都没登记即测试红，把「靠人记」变成「测试即失败」。`aiQueue`/`aiSessions` 如实登记为**边界**而非强行套水位——归档会把尚在 `PENDING` 的任务移出队列（任务静默不执行，比无界增长更糟），正解是 queue 层终态清理/TTL，属独立批次。**④ D3（A/B/隔离失效）** `evidence.js`（快照根）与 `profileMetrics.js`（profiles/proxies）是全项目**仅有**的两个完全不认 `FPB_DATA_DIR` 的模块 → 隔离测试把截图写进**真实** data 目录并长期累积，`profileMetrics` 还会读到与走 `dataRoot()` 的 `db.js` 不同的一份数据（C59 数据根分裂同族残留）。改为 `dataRoot()` 驱动；**非隔离模式下与旧硬编码值逐字相同 → 行为零变化**。夹具 `test_c79` 的快照根随隔离 `dataDir` 推导（`P6`/`P7` **断言一字未改**，17-A 同类处置：修夹具跟随新语义）。**⑤ D4（B/非原子写）** `backup.js:85` 恢复写回用裸 `writeFileSync` → 中断留半截 JSON，而该半截文件正是 D1 损坏分支的输入，串成「备份恢复中断 → 该集合被下次 RMW 静默清空」的连锁 → 改 `atomicWriteFileSync`。**守护测试 `test_c115_storage_durability`（33 断言，tmp 隔离/零浏览器/零 LLM）**：A 侧车保全 10（含 RMW 全链路可取证、同秒两次损坏互不覆盖）/ B 水位 5 / C 治理 5（含「`gaps()` 真遍历 `FILES` 并逐张表查询」静态锚，防恒空假绿）/ D 数据根 7（隔离验证走**真实子进程**，非隔离断言亦用子进程删 env 验证）/ E 原子写 4（**真实调用** `restoreSnapshot`）/ F T24 隔离零污染 2。**双向验证**：临时把侧车调用换成 no-op → `A2/A3/A4/A6/A9` **5 条精确红**、`sidecars=0`、其余 28 条不动，随后恢复 **33/0**。双回归 **223/0（869.0s）+ OK=216/BAD=0**，前后 treehash `OK=17 BAD=0` 零污染。
- **C114（step22 测试自身 stepId 子串碰撞修复，flake 三件之一正解落地）**：`test_step22_business_e2e` E 区用 `indexOf('e3')` 过滤 `task.step_started` 事件，但 stepId 真实格式 = `<taskId>_<planStepId>`（`stepManager.createStep` 拼 taskId 前缀，事件与 aiAttempts 同源）——taskId 为随机 base36，本身含 `e3` 子串时（约 1%）该任务全部步骤都被命中 → `started3[0]` 落在 e1 → aiAttempts 按 e1 过滤 → `e3Fails=0` 假红（5 轮 1 中）。修复：后缀锚定正则 `/_e3(_rp\d+)?$/`（兼容 replan 变体 `uniqueStepId` 的 `_e3_rpN`），与同文件 F 区既有 `endsWith('_f2')` 同模式。守护测试 `test_c114_step22_id_collision` **15/0**（真实模块契约 P1 + 碰撞形态旧假红机制精确复现/新谓词恰中 P2 + 干净路径零行为变化 P3 + replan 边界与不误吞 P4 + stripComments 源码锚点 P5 + T24 隔离零污染；零浏览器零 LLM）。无生产代码改动、无 client 改动跳过构建。双回归 **222/0（738.7s）+ OK=215/BAD=0** 双历史最佳（`.benchmark/c114_runregression.log` / `c114_phase9.log`）。
- **可靠性**：v2 池 100 任务 × 真实 deepseek：run3b→run6 = 95% → 97% → 98% → **99% SUCCESS**（唯一非 SUCCESS = CREDIBLE_BUSINESS 可信升级，按设计工作）
- **回归护栏**：全绿基线 runRegression **190/0** + phase9 **OK=183/BAD=0**（C92 口径；当前口径 runRegression **198/0**（397.3s）+ phase9 **OK=191/BAD=0** 全绿——**C94 safe_port 批次收口**（治理「临时端口随机命中 Chrome unsafe-port 黑名单 → page.goto ERR_UNSAFE_PORT 概率性假红」：lib_safe_port 共享原语 + 15 处测试迁移；收口时发现实现已由「绑定后校验重绑」改版为「显式候选端口」，而守护测试仍停旧契约 → B2 FATAL 无统计行，测试侧同步重写 B2 EADDRINUSE 重绑/B3 候选耗尽 reject/B4 5000 次采样守卫，并修 P1 误列 445（不在 Chromium kRestrictedPorts）与 P3a 整类守卫自指死结；test_c94 43/0）；C105 后口径；**C105 REAL-WEB AGENT RELIABILITY M1：F1-F6 落地 + 双回归归因修复**——F1 语义兜底词法关联收紧（零证据拒点，C105 法语站 Plateforme 误点机器根因）、F2 selector 接地判定降级为可观测标记 staleSelectorSuspected 不弃用（首轮回归实证：观察快照无法区分「selector 过期」与「目标尚未挂载」，弃用导致 step22 Scenario E 延时按钮被误判 → 语义兜底解析出合成 id #el-N → 30s 超时 → reload → BLANK 页死亡螺旋 → HUMAN_ESCALATION；D-B 死循环防护由 F5+F5b+repair 有界收口承担）、F3 P2 恒真判定表面化（host+pathname，query 不参与——pscd=try.webflow.com query 注入不再把真导航证据误判恒真）+ urlSurfaceKey 非法 URL 回退剥 query/hash 原始串（守卫不再静默失效，test_verification_invalid_evidence 28/28）、F4 replan 产出 selector 接地净化剥离留痕、F5 anti-flapping 同签名熔断 FLAP_THRESHOLD=4（阈值 2 会截断延时按钮「3 败 1 成」合法瞬时窗口——step22 63/0 实证修订）+ F5b reload 每 step 上限 1 次、F6 恢复词源接地（elementMissing 变体须在当前观察可解析）；test_c105_reliability 19/19 回放 fixture 守护 + matchedby 6/6 + 根目录 test_resolver 17/17（case7 改零证据拒点新契约）+ variant_cap 8/8；C104b replan gate unlock（runtime/planner 悬挂改动）随本批一并收口。**M2 grounding 增强**：data-testid/data-test/data-qa 与锚点 href（pathname 剥 query 防 token 泄漏）接入四层链路——observation 提取 → scoreField TIER 顶层（1.0，与 id 同级）/scoreSemantic 语义池/F1 兜底身份 → selectorFor 生成（testid 最高优先压过 id；无文本锚点 a[href*=pathname] 子串形态）→ selectorGrounded 接地快路 + cssGroundedInObs 复合属性面（data-testid/href），test_c105_m2_grounding 7/7 零浏览器守护；bbox/hit-test 属浏览器行为面归 M3 fixture 矩阵；**M3 REAL-WEB fixture 矩阵（Test A–J + Negative Test，26/26）落地**：observation 对可交互元素做中心点 elementFromPoint hit-test（输出 hitTest=clear/occluded/offscreen/zero-area/unknown，offscreen/unknown 一律不否决——防 M1 F2「快照分不清未挂载 vs 不存在」覆辙）+ semanticResolver 可操作性地面守卫（F7 零面积出局 / F8 occluded 出局，**全阻断回落保留候选并打 blockedBy**，空集会被上层误读成「元素不存在」而真实语义是「点不到」）+ verification 的 element_present/element_absent/field_value 改走 requireActionable:false 存在性通道（点不到 ≠ 不存在）+ href pathname 进语义池（无文本锚点唯一身份来源，且是语言中立信号——英文 signup 命中法语页 CTA 属正确行为）；矩阵覆盖法语落地页（C105 主诉复刻）/中文/overlay 遮挡不点击/延时挂载/零面积/联盟参数 URL/data-testid icon/href 锚点/offscreen 放行/遮挡 vs 干净；test_c105_m3_realweb_matrix 26/26（本地 fixture + 真实 Chromium 零 mock）+ step22 E2E 63/0；**登记边界**：F3 只解决 query 污染，子域 host 子串（try.webflow.com ⊃ webflow.com）仍判恒真——修它需重定义「到达站点」语义触 C103 面，已用断言固化防意外变更。**C105 真实站点复跑（4 轮，授权执行）**：新增 run_webflow_real_e2e.js 驱动真实 server 跑联盟注册任务，逐轮归因暴露四个本地 fixture 完全未覆盖的确定性缺陷并最小修复——F9 planner 语义语言契约（中文意译语义在英文/法文页零词法交集 → 解析零候选 + element_present 验证恒失败，实证 4 次 VERIFY_FAILED）、F10 合成 id 拒用（selectorFor 兜底 #el-N 是 observation 逻辑索引不是 DOM id → 30s 超时螺旋；改为无锚点返回 null + 执行面统一拒用）、F11 计划契约（field 臆造 CTA 文案/编造 DOM id + navigate 证据用入口域名被 P2 判恒真）、F14 重复 id 可见实例选择（重复 id 首个实例常是隐藏副本 w=0，探针实证落地页 #continue-nav 4 实例首个 w=0 → .first() 误报 not found ×18）、F13 熔断签名抗变体轮换（field 恒定 semantic 轮换使旧签名永远「新」→ 同字段重试 12 次/473s）。进展可测：第 1 轮 HUMAN_ESCALATION（点不进入口）→ 第 4 轮 FAILED（成功点进注册入口、22 步 63 动作、卡在分步表单填密码——Webflow 注册为分步表单，属规划能力边界非工程缺陷）；守护 test_c105_f10_f11_realweb 11/11 + test_c105_f14_duplicate_id 4/4（零浏览器 fake page）。**C106 分步表单推进（F15）**：真实站点第 4 轮卡点归因——注册是分步表单（邮箱 → 继续 → 密码），planner 假设单页表单 → `fill password` 时字段**尚未挂载** → ELEMENT_NOT_FOUND → 重试/replan/熔断全在重放同一步（实证同字段 12 次 / 473s → FAILED）。语义缺口是 ELEMENT_NOT_FOUND 混淆了「目标不存在」（该 replan）与「目标尚未出现」（该先推进再重查）。修：新模块 server/agent/stagedFormAdvance.js（字段类动作 + ELEMENT_NOT_FOUND + 未熔断 + 有预算时，点保守前进词表解析出的控件后重查目标；每 step 上限 2 次；**点击一次即止**，推进后字段仍不出现立即停止绝不连点；词表经危险词过滤）；runtime 主循环插入于 flap 记账后、重试决策前，推进成功重置 flap 记账（否则被 F5 熔断误伤），全程 agent.staged_form_advance 事件留痕。**真实浏览器 fixture 打脸 stub**：场景「页面只有 email 输入框」下，词表原含复合词 "Continue with email" 与输入框词法交集 → 把 email 输入框本身解析成前进控件并点击（stub 完全测不出）；修法两道：词表禁含字段名复合词（新增 P3d 整类守卫）+ 结构过滤 isAdvanceControl（只认 button/a/input[type=submit|button|image]/role=button|link，拒绝一切输入框/textarea/select/reset）——**语义评分负责排序、结构过滤负责合法性，二者不可互相替代**。守护 test_c106_f15_staged_advance 49/0（零浏览器：触发面/词表/结构合法性 + A–H 行为杀手含「输入框不得被点」+ 整类守卫）、test_c106_f15_staged_fixture 13/0（真实 Chromium + 真实 observation/semanticResolver + 真实点击：分步推进成功 / 死路只点一次 / 无控件零点击）。**C106 M2 真实站点第 5 轮归因（F16/F17/F18）**：第 5 轮真实 E2E（task_mtuqje3txasfd，283.5s → HUMAN_ESCALATION）经 execution.actions + aiEvents + 两个只读探针交叉取证，暴露三个本地 fixture 零覆盖的确定性缺陷——**F16「填错字段」型假成功**（`fill {semantic:'email', field:'password'}` 值被写进 email 框并判 SUCCESS：field 解析不到时评分器退而接受 semantic 命中的另一个可填字段；对 write 动作写错字段比找不到更危险，故 target.field 与 semantic 不等价时候选必须自带 field 证据——属性命中 token 或 input type 语义等价，否则拒绝并留痕 fieldMismatchRejected）、**F17「观察全崩=全盲」**（walk 内 `el.tagName.toLowerCase()` 在真实页抛 TypeError → inspect 返回 `{ok:false}` 且**无 observation** → 所有动作一律报「未找到」，与页面真实内容无关、F15 也恒报 no_advance_control；修法三层：tagName 空值防御 + `walk` 外层 try 截断保底（保留已采集元素 + walkError 留痕，降级为「部分可见」而非「全盲」）+ fill/click 分支 `obs.ok===false` 报 OBSERVATION_FAILED fail-loud，**观察失败 ≠ 元素不存在**，否则恢复分支走错）、**F18 反爬挑战页识别**（探针实证 webflow.com/signup 落地其实是 PerimeterX 挑战页「Confirm you're not a bot / press and hold」，DOM 仅 22–26 节点、零 input，而 agent 在其上反复填注册表单空转 283s 且升级理由完全误导；新模块 server/agent/botChallenge.js 文本多语言 + 供应商组件 + URL 三通道、置信度分层，runtime 命中即 escalate(reason='BOT_CHALLENGE')，**只识别+交给人，绝不解题/绝不隐藏自动化痕迹/绝不触碰挑战控件**）。守护 test_c106_f16_f17_unit 17/0 + test_c106_f16_f17_fixture 13/0（真实 Chromium：坏元素容错 / 填对字段 / 拒绝填错字段 / 观察失败零写入）+ test_c106_f18_bot_challenge 34/0（多语言文本 + 7 家供应商 + 负例防误判 + 反站点特判整类守卫）+ test_c106_f18_bot_challenge_fixture 6/0。⚠️ **红线处置**：真实 Webflow 注册路径已被 PX 人机验证拦截，任何自动化通过尝试都属 CAPTCHA/PX/WAF bypass，故停止真实注册 E2E，改由 F18 明确上报「需真人通过验证」。**C106 M3 人类化输入（F20，用户现场观察驱动）**：用户现场观察「注册第一步输入邮箱，输入太快 → 界面刷新 → 只输了一小部分 → 页面又刷新 → 又把密码输入到邮箱里」，归因出三个 execution 层缺陷——**F20-a 注入节奏**（fill 走 `humanType(baseDelay:30, randomDelay:60)` 均值 60ms/字符 ≈17 字符/秒，而人类是 150–250ms/字符；新模块 server/agent/humanInput.js 的 typingProfile 给出 base 90 + rand 0–110（均值 ~145ms）、凭据类字段 ×1.25，**>120 字符降级 mode:'fill' 整体赋值**——长文本对应人类「粘贴」，否则地板 25ms × 4000 字符 = 100s 会击穿 withBrowserOp 的 25s 超时，此缺陷由守护测试跑出而非设计预设）、**F20-b 输入前稳定等待**（waitFieldStable：元素两次采样尺寸/可见/非 disabled 一致，上限 1.5s，避开受控组件 re-render 窗口）、**F20-c 输入后回读校验**（humanType 末尾回读 DOM 值，不等则「重聚焦 + 全选清空 + 重输」一次，仍不等 → tools 报 FILL_VALUE_MISMATCH fail-loud；**此前 fill 从不回读，只进去几个字符也判 SUCCESS**，这正是错误能一路静默传到验证层的根因）。关键取舍：循环中间**绝不回退索引重输**（受控组件 setState 异步，中间回读落后于实际输入会造成字符重复，宁可慢不可错），只在末尾补录；valuesMatch 对非敏感字段允许前端格式化（去分隔符后相等视为成功），**密码/CVV/卡号绝不宽松**（空格是有效字符）。守护 test_c106_f20_human_input 32/0（零浏览器：节奏与预算/等价判定/凭据掩码/稳定判定 + 纪律锁「无站点名、无绕过动词、不触碰 verification 判据面」+ 6 条接入面断言防「改模块不接线」）、test_c106_f20_typing_fixture 13/0（真实 Chromium：**C1 对照组复现用户现象**——受控组件 re-render 重建 input 致焦点丢失时，关守卫只进去 "user@" 5 字符、开守卫补录成完整值；含超长值降级、死路 fail-loud、格式化不误报）。**C106 M4 网络就绪判据分层（F19，破除 WAIT 死循环）**：第 6 轮真实 E2E 实证 27 个动作全卡 step_001，固定循环 `networkState=pending → EVENTUAL_CONSISTENCY → WAIT → 误诊 NETWORK_REQUEST_FAILED(conf 0.9) → wait`。两个根因均为确定性缺陷——**判据语义**：请求计数器把所有 request 一视同仁（document/xhr/fetch/长轮询/analytics beacon/websocket 全 +1），真实站点（SPA + analytics + 长轮询）pending 恒为真 → `verificationIntelligence._analyze` 第 2 步短路命中 WAIT → 第 3 步起整条判据链（loading / domChanged / 证据）永远走不到；**挂载时机**（比恒 pending 更危险）：`ensureNetHook` 原先只在首次 inspect 才挂 → 页面加载期间的 document / 阻塞脚本 / 首屏 xhr 全部漏计 → 「没抓到」会**伪装成已就绪**。修：抽独立模块 server/agent/networkReadiness.js（避免 browserManager 反向依赖 agent/observation），按请求类型给阻塞窗口（document/script/stylesheet 20s、xhr/fetch 3s 首屏窗口，ping(beacon)/eventsource/websocket/image/font/media 一律不计入）；browserManager 新增 `newTrackedPage()` 统一入口（5 处 newPage 全替换）**page 创建即 attach**；observation 收敛为委托。守住「该等的时候要等」：首屏 xhr 在 3s 窗口内仍判 pending、主 frame 阻塞脚本长挂仍判 pending、iframe 子 frame 长挂不钉住主页面。踩坑两则：sed 批量替换把 helper 内部也换了 → `newTrackedPage` 自递归（grep 落盘验证抓到，已加 F5 断言防复发）；fake page 无 `page.on` 但 attach 已先建空分层表 → 堵死 `__pendingRequests` 回退路径（改「不能监听就绝不建表」）。守护 test_c106_f19_network_readiness 13/0（真实 Chromium + 真实挂起请求：长轮询 4s 后 idle 且有未完成请求佐证为真阳性 / 首屏 xhr 窗口内 pending / 阻塞脚本 pending / iframe 不钉住 / 验证链不再短路 + 源码纪律锁）。前一交付 C92：custom geolocation 非对称校验缺陷修复（test_c92 21/0，详见 git 历史 e027450 后日志））
- **交付 C83 — 审计覆盖对账（server/index.js 面）**：37 条 mutation 路由全量枚举，6 条真实审计链断裂补埋点 —— browser.evaluate（RCE 等价面）/ navigate / human-click/type/google-search（会话驱动面）/ cookies import（认证态注入）/ export（cookie exfil 面）/ automation/run 成功+失败双路径（业务关键 mutation）；detail 只记长度/数量（凭据明文红线），冻结 allowlist 豁免 5 条无状态预览/诊断面 + 2 条高频拟人流（环形缓冲冲刷边界），test_c83 72/0
- **交付 C85 — /schedules 子路由审计闭环（scheduleTrigger.js 面，C84 登记的收尾候选）**：C84 闭环 agent/index.js 34 面后，/schedules 独立子路由（自带守卫也自带审计盲区）create/update/delete/trigger 四面零审计；补 4 处 audit.logRequest 埋点（ai.schedule.*），意图归因同款（resourceId=scheduleId，运行细节归 events/trace）；tick 高频自动触发不逐次审计（环形缓冲冲刷边界，events.emit('schedule.triggered') 已覆盖）仅手动 trigger 落审计，P2c 结构断言固化「审计只在 HTTP 意图层、模块层禁调」；objective/targetUrl 明文不入 detail（C81 红线），无效创建 400 不落审计（无实体即无意图实现），跨工作区 403 守卫先于埋点，test_c85 41/0
- **交付 C84 — AI 面审计闭环（agent/index.js 面，C83 登记的后续批次）**：/api/ai/* 全部 34 条 mutation 路由此前零审计（AI 任务全生命周期在安全审计链不可见），补 28 处埋点 + 2 既有 secrets + 4 冻结豁免 = 34 面闭环；意图归因设计——审计只补「谁在何时对哪个任务做了什么」（resourceId=taskId），运行细节仍归 trace/aiSteps 证据链；/chat 审计点锚在任务创建：mock 规划恒失败（C79 已证边界）→ 400 响应但意图审计落盘（行为面最强实证）；聊天/暂停原因明文只记 *Len，modify 只记 patch 字段名（C81 红线）；audit.logRequest 共享原语提升（index.js auditReq 与 agent aiAudit 同源，C62 fsSafe 纪律），test_c84 102/0
- **交付 C82 — 浮动 async handler 悬挂清退**：Express 4 不接 rejection，`/automation/run`（runWorkflow 设计性抛错契约 → 悬挂至超时 UI 零反馈，真实 B 类）+ profiles GET/PUT/duplicate/preview-fp + proxies check-geo + browser stop 共 7 处统一 try/catch 固化「路由层永远回 JSON 不悬挂」契约，pre-fix TIMEOUT 悬挂实录 → post-fix 快速 500 JSON；P3 结构化守卫全量扫描 async handler 防整类回归，test_c82 18/0
- **交付 C50 — N-XCONS 双层一致性守护**：native identity.json 驱动下 JS↔HTTP Client Hints 全链实测（test_fp16b_nxcons.js，stock 4/0 + patched 9/0 + 1 边界留痕）——platformVersion 双层承诺实证成立（C2 头层真实跟随）；UA/platform/brands 未驱动面原生同源无断裂；边界 W1（navigator.platform JS 层 vs sec-ch-ua-platform 头 OS 面）显式留痕为 POC #3 冻结单面设计，OS 面联动列为后续扩展。Chrome 151→152 漂移实锤：getHighEntropyValue 单数 API 已移除（存量资产零击穿，全部已用复数）。详见 `.benchmark/C50_NXCONS_CROSS_CONSISTENCY.md`
- **端点×UI 对账主线完案（C22–C36）**：server 路由与 client 消费差全部闭合或判定不做；遗留端点（/ai/queue、/ai/events、/ai/observability/metrics）已带 RFC 8594 deprecation 标记；/auth/register+login 评估结论 = local 自动身份 + 治理中心建号已闭环 readiness auth 项，不新增登录页
- **身份架构**：16-B 全家族收口（6 Native ACTIVE + languages CONFIG + brands/screen CLOSED，详见 `.benchmark/PHASE16B_ROI_GATE.md`）
- **交付**：C35 凭据引用治理 UI（credentialRef 注册/删除/脱敏视图，明文闭环走 Profile 编辑器 vault）+ 任务详情手动恢复按钮；A 类缺陷修复：maskedView 的 available 改为 vault 只读现算（修复「先注册后补录明文 → 列表与 Planner 永远显示不可用」）
- **交付**：C29 执行引擎补齐（提交执行 / 崩溃恢复 / 资源池获取释放 / 动作契约与策略只读调试）
- **交付**：C28 Intelligence 决策试算 + 经验健康看板上线；C27 任务取证四件套（诊断/修复/执行/重放）上线
- **交付**：C26 治理中心上线 —— API Key 自管 / 安全审计日志 / 工作空间与成员 RBAC（C25 经验包导出导入、C24 智能记忆面板、C23 执行引擎面板、C22 数据备份恢复 均已上线）
- **交付**：C14 系统设置中心上线 —— API key / 模型配置 UI 化（密文落盘 + 保存即生效 + 连通测试 + env 对账）

## 合规与安全
- 指纹伪装 + 自动化是**双用途**能力：适用于自测注册/支付流程、管理你拥有或获明确授权的账号、无障碍自动化等合法场景。
- **请只对你拥有或获明确授权的账号/卡号使用，并遵守目标网站的 ToS。**
- 本项目**不**提供任何规避支付风控、CAPTCHA/3DS、盗卡测试、批量薅羊毛的专门设计；LLM 侧凭据判定链保证模型永不见明文 CVV/卡号。
- 卡号/CVV 为敏感数据：务必设置 `FPB_MASTER_KEY`；`data/vault.json` 仅存密文。`.env` 与 `data/` 已被 gitignore。

## 环境变量（.env）
| 变量 | 必填 | 说明 |
|------|------|------|
| `DEEPSEEK_API_KEY` | AI 任务必填 | DeepSeek API key（**推荐直接在 UI「系统设置」里配置**，密文落盘、保存即生效；缺省时 AI 任务 fail-fast，Profile 管理不受影响） |
| `FPB_MASTER_KEY` | 建议 | 保险库主密钥（base64 32 字节）；不设则一次性内存密钥 |
| `FPB_NATIVE_CHROME` | 可选 | 指向 native patched chrome.exe（启用 16-B Native 身份架构） |
| `FPB_SCENARIO_DIR` / `FPB_POOL_FILE` | 可选 | 基准任务池覆盖（v2 池） |

完整变量清单见 `.env.example`。
