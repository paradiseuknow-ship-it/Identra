# =============================================================================
# fingerprint-browser / AI Browser Operator
# =============================================================================
# 分区：依赖 / 构建产物 / 运行时数据 / 密钥 / 日志 / 评测产物 / 临时脚本 / 备份 / 系统
# 原则：任何「含凭据」「含用户数据」「机器生成」的东西一律不入库。
# -----------------------------------------------------------------------------

# ---- 1. 依赖（任意层级）----------------------------------------------------
node_modules/
.pnp/
.pnp.js

# ---- 2. 构建产物 -----------------------------------------------------------
client/dist/
dist/
build/
out/
*.tsbuildinfo
.vite/

# ---- 3. 运行时数据（含加密凭据与业务数据，绝不入库）-------------------------
# db.js   -> /data/          (profiles.json / proxies.json / tasks.json)
# AI store-> /server/data/   (aiCredentials / aiPaymentMethods / aiExecutions ...)
/data/
/server/data/
**/downloads/
*.db
*.sqlite
*.sqlite3

# ---- 4. 密钥与本地配置（绝不入库）-----------------------------------------
.env
.env.*
!.env.example
*.key
*.pem
*.p12
*.pfx
*.crt
secrets/
*.local
master.key

# ---- 5. 日志 ---------------------------------------------------------------
*.log
logs/
_*.log
npm-debug.log*
yarn-error.log*

# ---- 6. 评测 / 基准 / 取证产物（体积大且可再生）---------------------------
.benchmark/
.cap/
benchmark/results/
*.ndjson

# ---- 7. 一次性脚本与临时文件（下划线前缀 = 临时约定）----------------------
_*.js
_*.json
_*.txt

# ---- 8. 备份与转储 ---------------------------------------------------------
**/*_backup/
*.bak
*.tmp

# ---- 9. 操作系统 / 编辑器 --------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
*~

# ---- 10. WorkBuddy 会话数据（工具内部状态，非产品源码）--------------------
.workbuddy/
