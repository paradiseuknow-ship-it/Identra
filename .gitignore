# =============================================================================
# fingerprint-browser / AI Browser Operator
# =============================================================================
# 分区：依赖 / 构建产物 / 运行时数据 / 密钥 / 日志 / 评测产物 / 临时脚本 / 备份 / 系统
# 原则：任何「含凭据」「含用户数据」「机器生成」的东西一律不入库。
# -----------------------------------------------------------------------------

# ---- 1. 依赖（任意层级）----------------------------------------------------
node_modules/
.pnp/
.pnp.js

# ---- 2. 构建产物 -----------------------------------------------------------
client/dist/
dist/
build/
out/
*.tsbuildinfo
.vite/

# ---- 3. 运行时数据（含加密凭据与业务数据，绝不入库）-------------------------
# db.js   -> /data/          (profiles.json / proxies.json / tasks.json)
# AI store-> /server/data/   (aiCredentials / aiPaymentMethods / aiExecutions ...)
/data/
/server/data/
**/downloads/
/server/agent/data/
*.db
*.sqlite
*.sqlite3

# ---- 4. 密钥与本地配置（绝不入库）-----------------------------------------
.env
.env.*
!.env.example
*.key
*.pem
*.p12
*.pfx
*.crt
secrets/
*.local
master.key

# ---- 5. 日志 ---------------------------------------------------------------
*.log
logs/
_*.log
npm-debug.log*
yarn-error.log*

# ---- 6. 评测 / 基准 / 取证产物（体积大且可再生）---------------------------
.benchmark/
.cap/
benchmark/results/
*.ndjson

# ---- 7. 一次性脚本与临时文件（下划线前缀 = 临时约定）----------------------
_*.js
_*.json
_*.txt
# 例外：_testSite.js 是**常驻共享依赖**（server/scripts/testAgentPhase5/22/23/31.js 四个已入库
# 套件都 require('./_testSite') + server/test-site/server.js 的 VERSION 必须与之成对），
# 被上面 `_*.js` 的「一次性脚本」通配误伤 ⇒ 新 clone 跑任何浏览器套件都 MODULE_NOT_FOUND。
# C140 归因副产物：VERSION 常量在两处必须同步演进，其中一处不入库则配对无法保证。
!server/scripts/_testSite.js
tmp_*.js
vite.config.js.timestamp-*.mjs

# ---- 8. 备份与转储 ---------------------------------------------------------
**/*_backup/
*.bak
*.tmp

# ---- 9. 操作系统 / 编辑器 --------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
*~

# ---- 10. WorkBuddy 会话数据（工具内部状态，非产品源码）--------------------
.workbuddy/
.step22-e2e/
release/

# ---- 11. C90 数据清理备份（运行时数据快照，绝不入库）------------------------
_cleanup_backup_*/
